<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>肥言肥语</title>
  
  <subtitle>肥肥鱼胡说八道的地方</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://fiissh.tech/"/>
  <updated>2019-04-16T07:04:32.000Z</updated>
  <id>https://fiissh.tech/</id>
  
  <author>
    <name>fiissh.zhao</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Flutter 开发（14）：BasicMessageChannel 的使用</title>
    <link href="https://fiissh.tech/2019/flutter-basic-message-channel.html"/>
    <id>https://fiissh.tech/2019/flutter-basic-message-channel.html</id>
    <published>2019-04-16T07:04:32.000Z</published>
    <updated>2019-04-16T07:04:32.000Z</updated>
    
    <content type="html"><![CDATA[<p>与 <code>MethodChannel</code> 和 <code>EventChannel</code> 接收一个 <code>MethodCodec</code> 对象所不同的是，<code>BasicMessageChannel</code> 接收一个 <code>MessageCodec&lt;T&gt;</code> 作为其编解码器的实现。<code>BasicMessageChannel</code> 允许我们使用自定义的消息编解码器进行异步消息传递。</p><p>本文中所使用的代码示例出自 <code>Flutter</code> 官方示例，感兴趣的小伙伴请移步 <a href="https://github.com/flutter/flutter/tree/master/examples/flutter_view" target="_blank" rel="noopener">flutter/examples/flutter_view</a></p><a id="more"></a><h2 id="BasicMessageChannel-的初始化"><a href="#BasicMessageChannel-的初始化" class="headerlink" title="BasicMessageChannel 的初始化"></a>BasicMessageChannel 的初始化</h2><p><code>BasicMessageChannel</code> 不提供默认的 <code>MessageCodec</code> 的实现，我们需要在初始化 <code>BasicMessageChannel</code> 时指定具体的 <code>MessageCodec</code>。我们以使用 <code>StringCodec</code> 初始化 <code>BasicMessageChannel</code> 并传递 <code>String</code> 类型的数据为例：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> BasicMessageChannel&lt;<span class="built_in">String</span>&gt; platform = BasicMessageChannel&lt;<span class="built_in">String</span>&gt;(_channel, StringCodec());</span><br></pre></td></tr></table></figure><p>对象初始化之后，我们通过 <code>setMessageHandler</code> 方法设置一个 <code>MessageHandler</code> 回调函数：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">platform.setMessageHandler(_handlePlatformIncrement);</span><br><span class="line">Future&lt;<span class="built_in">String</span>&gt; _handlePlatformIncrement(<span class="built_in">String</span> message) <span class="keyword">async</span> &#123;</span><br><span class="line">  setState(() &#123;</span><br><span class="line">    _counter++;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> _emptyMessage;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>初始化完成之后，我们可以通过 <code>platform.send(&#39;pong&#39;);</code> 向设备平台端发送一个 <code>String</code> 类型的数据。</p><h2 id="设备平台-Android-端的初始化"><a href="#设备平台-Android-端的初始化" class="headerlink" title="设备平台 Android 端的初始化"></a>设备平台 Android 端的初始化</h2><p>相应的，我们需要在设备平台端（<code>Android</code>） 初始化 <code>BasicMessageChannel</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">messageChannel = <span class="keyword">new</span> BasicMessageChannel&lt;&gt;(flutterView, CHANNEL, StringCodec.INSTANCE);</span><br><span class="line">messageChannel.</span><br><span class="line">    setMessageHandler(<span class="keyword">new</span> MessageHandler&lt;String&gt;() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onMessage</span><span class="params">(String s, Reply&lt;String&gt; reply)</span> </span>&#123;</span><br><span class="line">            onFlutterIncrement();</span><br><span class="line">            reply.reply(EMPTY_MESSAGE);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure><p>设备平台端收到数据之后，<code>onMessage</code> 方法会被触发。</p><p>同样的，在设备平台端可以使用 <code>messageChannel.send(PING);</code> 向 <code>Flutter</code> 端发送 <code>String</code> 类型的数据，<code>Flutter</code> 端收到数据之后，<code>_handlePlatformIncrement</code> 方法会被触发。</p><h2 id="BasicMessageChannel-的原理"><a href="#BasicMessageChannel-的原理" class="headerlink" title="BasicMessageChannel 的原理"></a>BasicMessageChannel 的原理</h2><p>与 <code>MethodChannel</code> 一样，<code>BasicMessageChannel</code> 在发送数据时需要先通过 <code>codec</code> 将消息编码，然后通过 <code>BinaryMessages</code> 将编码之后的消息发送出去，最后通过 <code>codec</code> 再将返回值进行解码：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;T&gt; send(T message) <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> codec.decodeMessage(<span class="keyword">await</span> BinaryMessages.send(name, codec.encodeMessage(message)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>BasicMessageChannel</code> 提供了一种机制允许我们自己定义消息的编解码器。相应的，<code>Flutter</code> 中已经提供了 <code>BinaryCodec</code>、<code>StringCodec</code> 和 <code>JSONMessageCodec</code> 作为常用数据类型的编解码器实现。如果还不能满足需求，我们可以通过扩展 <code>MessageCodec&lt;T&gt;</code> 达到目的。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;与 &lt;code&gt;MethodChannel&lt;/code&gt; 和 &lt;code&gt;EventChannel&lt;/code&gt; 接收一个 &lt;code&gt;MethodCodec&lt;/code&gt; 对象所不同的是，&lt;code&gt;BasicMessageChannel&lt;/code&gt; 接收一个 &lt;code&gt;MessageCodec&amp;lt;T&amp;gt;&lt;/code&gt; 作为其编解码器的实现。&lt;code&gt;BasicMessageChannel&lt;/code&gt; 允许我们使用自定义的消息编解码器进行异步消息传递。&lt;/p&gt;
&lt;p&gt;本文中所使用的代码示例出自 &lt;code&gt;Flutter&lt;/code&gt; 官方示例，感兴趣的小伙伴请移步 &lt;a href=&quot;https://github.com/flutter/flutter/tree/master/examples/flutter_view&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;flutter/examples/flutter_view&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="Flutter" scheme="https://fiissh.tech/categories/flutter/"/>
    
    
      <category term="BasicMessageChannel" scheme="https://fiissh.tech/tags/basicmessagechannel/"/>
    
  </entry>
  
  <entry>
    <title>Flutter 开发（13）：EventChannel 的使用</title>
    <link href="https://fiissh.tech/2019/flutter-event-channel.html"/>
    <id>https://fiissh.tech/2019/flutter-event-channel.html</id>
    <published>2019-04-16T03:22:38.000Z</published>
    <updated>2019-04-16T03:22:38.000Z</updated>
    
    <content type="html"><![CDATA[<p><code>EventChannel</code> 适用于 <code>Flutter</code> 端和设备平台端数据事件的通信。比较常见的使用 <code>EventChannel</code> 的场景是通过广播监听（<code>Android</code> 平台上的 <code>BroadcastReceiver</code>）动态的监听设备事件。</p><p>本文中的示例代码是 <code>Flutter</code> 官方示例，感兴趣的小伙伴可以移步 <a href="https://github.com/flutter/flutter/tree/master/examples/platform_channel" target="_blank" rel="noopener">flutter/examples/platform_channel/</a>。<br><a id="more"></a></p><h2 id="EventChannel-的初始化"><a href="#EventChannel-的初始化" class="headerlink" title="EventChannel 的初始化"></a>EventChannel 的初始化</h2><p>与 <code>MethodChannel</code> 相类似的，<code>EventChannel</code> 也需要一个全局唯一的 <code>name</code> 属性作为其标识符：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> EventChannel eventChannel = EventChannel(<span class="string">'samples.flutter.io/charging'</span>);</span><br></pre></td></tr></table></figure><p>创建 <code>EventChannel</code> 的对象之后，我们需要向 <code>EventChannel</code> 注册一个监听回调方法用于监听事件：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">eventChannel.receiveBroadcastStream().listen(_onEvent, onError: _onError);</span><br><span class="line"><span class="keyword">void</span> _onEvent(<span class="built_in">Object</span> event) &#123;</span><br><span class="line">  setState(() &#123;</span><br><span class="line">    _chargingStatus =</span><br><span class="line">        <span class="string">"Battery status: <span class="subst">$&#123;event == <span class="string">'charging'</span> ? <span class="string">''</span> : <span class="string">'dis'</span>&#125;</span>charging."</span>;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> _onError(<span class="built_in">Object</span> error) &#123;</span><br><span class="line">  setState(() &#123;</span><br><span class="line">    _chargingStatus = <span class="string">'Battery status: unknown.'</span>;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="设备平台-Android-端的初始化"><a href="#设备平台-Android-端的初始化" class="headerlink" title="设备平台 Android 端的初始化"></a>设备平台 Android 端的初始化</h2><p>在设备平台（<code>Android</code>）端，我们需要配置 <code>EventChannel</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> EventChannel(getFlutterView(), CHARGING_CHANNEL).setStreamHandler(</span><br><span class="line">    <span class="keyword">new</span> StreamHandler() &#123;</span><br><span class="line">      <span class="keyword">private</span> BroadcastReceiver chargingStateChangeReceiver;</span><br><span class="line">      <span class="meta">@Override</span></span><br><span class="line">      <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onListen</span><span class="params">(Object arguments, EventSink events)</span> </span>&#123;</span><br><span class="line">        chargingStateChangeReceiver = createChargingStateChangeReceiver(events);</span><br><span class="line">        registerReceiver(</span><br><span class="line">            chargingStateChangeReceiver, <span class="keyword">new</span> IntentFilter(Intent.ACTION_BATTERY_CHANGED));</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="meta">@Override</span></span><br><span class="line">      <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onCancel</span><span class="params">(Object arguments)</span> </span>&#123;</span><br><span class="line">        unregisterReceiver(chargingStateChangeReceiver);</span><br><span class="line">        chargingStateChangeReceiver = <span class="keyword">null</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>其中，<code>onListen</code> 是 <code>Flutter</code> 端设置监听回调方法之后调用，我们在该方法中触发时向系统注册监听电量变化的广播：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> BroadcastReceiver <span class="title">createChargingStateChangeReceiver</span><span class="params">(<span class="keyword">final</span> EventSink events)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> BroadcastReceiver() &#123;</span><br><span class="line">      <span class="meta">@Override</span></span><br><span class="line">      <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onReceive</span><span class="params">(Context context, Intent intent)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> status = intent.getIntExtra(BatteryManager.EXTRA_STATUS, -<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (status == BatteryManager.BATTERY_STATUS_UNKNOWN) &#123;</span><br><span class="line">          events.error(<span class="string">"UNAVAILABLE"</span>, <span class="string">"Charging status unavailable"</span>, <span class="keyword">null</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="keyword">boolean</span> isCharging = status == BatteryManager.BATTERY_STATUS_CHARGING ||</span><br><span class="line">                               status == BatteryManager.BATTERY_STATUS_FULL;</span><br><span class="line">          events.success(isCharging ? <span class="string">"charging"</span> : <span class="string">"discharging"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><p><code>onCancel</code> 是事件流断开时触发，我们在其中将广播取消。由此一来，设备平台端的事件变化可以通过 <code>EventChannel</code> 从设备平台端传递到 <code>Flutter</code> 端。</p><p>当设备平台端的事件发生变化时，通过 <code>EventSink</code> 对象 <code>events</code> 将对应的状态回传给 <code>Flutter</code> 端。</p><h2 id="EventChannel-的原理"><a href="#EventChannel-的原理" class="headerlink" title="EventChannel 的原理"></a>EventChannel 的原理</h2><p>事实上，<code>EventChannel</code> 是使用 <code>MethodChannel</code> 和 <code>Stream</code> 以达到监听事件变化的目的的：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">Stream&lt;<span class="keyword">dynamic</span>&gt; receiveBroadcastStream([<span class="keyword">dynamic</span> arguments]) &#123;</span><br><span class="line">  <span class="keyword">final</span> MethodChannel methodChannel = MethodChannel(name, codec);</span><br><span class="line">  StreamController&lt;<span class="keyword">dynamic</span>&gt; controller;</span><br><span class="line">  controller = StreamController&lt;<span class="keyword">dynamic</span>&gt;.broadcast(onListen: () <span class="keyword">async</span> &#123;</span><br><span class="line">    BinaryMessages.setMessageHandler(name, (ByteData reply) <span class="keyword">async</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (reply == <span class="keyword">null</span>) &#123;</span><br><span class="line">        controller.close();</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          controller.add(codec.decodeEnvelope(reply));</span><br><span class="line">        &#125; on PlatformException <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">          controller.addError(e);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">await</span> methodChannel.invokeMethod&lt;<span class="keyword">void</span>&gt;(<span class="string">'listen'</span>, arguments);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (exception, stack) &#123;</span><br><span class="line">      FlutterError.reportError(FlutterErrorDetails(</span><br><span class="line">        exception: exception,</span><br><span class="line">        stack: stack,</span><br><span class="line">        <span class="keyword">library</span>: <span class="string">'services library'</span>,</span><br><span class="line">        context: <span class="string">'while activating platform stream on channel <span class="subst">$name</span>'</span>,</span><br><span class="line">      ));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, onCancel: () <span class="keyword">async</span> &#123;</span><br><span class="line">    BinaryMessages.setMessageHandler(name, <span class="keyword">null</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">await</span> methodChannel.invokeMethod&lt;<span class="keyword">void</span>&gt;(<span class="string">'cancel'</span>, arguments);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (exception, stack) &#123;</span><br><span class="line">      FlutterError.reportError(FlutterErrorDetails(</span><br><span class="line">        exception: exception,</span><br><span class="line">        stack: stack,</span><br><span class="line">        <span class="keyword">library</span>: <span class="string">'services library'</span>,</span><br><span class="line">        context: <span class="string">'while de-activating platform stream on channel <span class="subst">$name</span>'</span>,</span><br><span class="line">      ));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> controller.stream;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们在 <code>Flutter</code> 端调用 <code>receiveBroadcastStream</code> 方法之后，会返回一个 <code>Stream</code> 对象用于接收数据流，其中 <code>listen</code> 方法则用于监听数据的变化。</p><p>而在 <code>receiveBroadcastStream</code> 方法调用时，其中通过 <code>MethodChannel</code> 调用了对应设备平台上的 <code>onListen</code> 和 <code>onCancel</code> 两个方法。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>与 <code>MethodChannel</code> 一样，<code>EventChannel</code> 最为核心的实现也是 <code>BinaryMessages</code> 和 <code>MethodCodec</code>。默认的，<code>EventChannel</code> 使用 <code>StandardMethodCodec</code> 作为 <code>codec</code> 属性的默认实现。</p><p>与 <code>MethodChannel</code> 一样，<code>EventChannel</code> 也可以使用 <code>JSONMethodCodec</code> 作为其编解码器的实现。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;code&gt;EventChannel&lt;/code&gt; 适用于 &lt;code&gt;Flutter&lt;/code&gt; 端和设备平台端数据事件的通信。比较常见的使用 &lt;code&gt;EventChannel&lt;/code&gt; 的场景是通过广播监听（&lt;code&gt;Android&lt;/code&gt; 平台上的 &lt;code&gt;BroadcastReceiver&lt;/code&gt;）动态的监听设备事件。&lt;/p&gt;
&lt;p&gt;本文中的示例代码是 &lt;code&gt;Flutter&lt;/code&gt; 官方示例，感兴趣的小伙伴可以移步 &lt;a href=&quot;https://github.com/flutter/flutter/tree/master/examples/platform_channel&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;flutter/examples/platform_channel/&lt;/a&gt;。&lt;br&gt;
    
    </summary>
    
      <category term="Flutter" scheme="https://fiissh.tech/categories/flutter/"/>
    
    
      <category term="EventChannel" scheme="https://fiissh.tech/tags/eventchannel/"/>
    
  </entry>
  
  <entry>
    <title>Flutter 开发（12）：MethodChannel 的使用</title>
    <link href="https://fiissh.tech/2019/flutter-method-channel.html"/>
    <id>https://fiissh.tech/2019/flutter-method-channel.html</id>
    <published>2019-04-15T06:15:12.000Z</published>
    <updated>2019-04-15T06:15:12.000Z</updated>
    
    <content type="html"><![CDATA[<p>在 <code>Flutter</code> 中，<code>MethodChannel</code> 允许我们发送与方法调用相关的消息。<code>MethodChannel</code> 适用于 <code>Flutter</code> 端和设备平台端进行直接的方法调用，这种方法调用是双向的，即既可以通过 <code>MethodChannel</code> 从 <code>Flutter</code> 调用设备平台上的方法，也可以从设备平台上调用 <code>Flutter</code> 层的代码。<br><a id="more"></a></p><h2 id="MethodChannel-实现方法调用的步骤"><a href="#MethodChannel-实现方法调用的步骤" class="headerlink" title="MethodChannel 实现方法调用的步骤"></a>MethodChannel 实现方法调用的步骤</h2><p>在 <code>Flutter</code> 层，我们需要通过 <code>MethodChannel</code> 的构造方法创建其对象，然后通过 <code>invokeMethod</code>、<code>invokeListMethod</code> 或者 <code>invokeMapMethod</code> 方法将需要在设备平台响应的方法名称和参数，通过 <code>BinaryMessages</code> 传递给设备平台对应的响应对象（<code>Android</code> 平台为 <code>MethodChannel</code>，<code>iOS</code> 平台为 <code>FlutterMethodChannel</code>），设备平台响应完成之后将结果传递回 <code>Flutter</code>。相应的，从设备平台调用 <code>Flutter</code> 层的方法也是一样的：</p><p><img src="https://upload-images.jianshu.io/upload_images/4313776-f4f8d3493b076946.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/580" alt></p><ul><li>首先，<code>MethodChannel</code> 收到方法调用的请求之后，先通过 <code>MethodCodec</code> 的 <code>encodeMethodCall</code> 方法将需要调用的方法名称和参数信息进行编码</li><li>然后，通过 <code>BinaryMessages.send</code> 方法将编码之后的数据传递给对应的设备平台，并接收其返回值</li><li>最后，根据返回结果的值进行处理：<ul><li>如果返回值为 <code>null</code>，抛出 <code>MissingPluginException</code></li><li>如果返回值不为 <code>null</code>，通过 <code>MethodCodec</code> 的 <code>decodeEnvelope</code> 将返回值解码为具体的类型</li></ul></li></ul><h2 id="Flutter-层的-MethodChannel"><a href="#Flutter-层的-MethodChannel" class="headerlink" title="Flutter 层的 MethodChannel"></a>Flutter 层的 MethodChannel</h2><h3 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h3><p><code>MethodChannel</code> 的构造方法接收一个 <code>name</code> 和一个可选的 <code>codec</code> 变量：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> MethodChannel(<span class="keyword">this</span>.name, [<span class="keyword">this</span>.codec = <span class="keyword">const</span> StandardMethodCodec()]);</span><br></pre></td></tr></table></figure><p>其中，<code>name</code> 是 <code>String</code> 类型，作用是为每一个 <code>MethodChannel</code> 设置一个唯一的 <code>ID</code>，<code>codec</code> 是 <code>MethodCodec</code> 类型，默认值是 <code>StandardMethodCodec</code>，作用是对方法调用和返回值进行编解码：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="built_in">String</span> _METHOD_CHANNEL_NAME = <span class="string">'fiissh.flutter.plugin/flutter_file_path_provide'</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> MethodChannel _channel = <span class="keyword">const</span> MethodChannel(_METHOD_CHANNEL_NAME);</span><br></pre></td></tr></table></figure><p>通常情况下，我们使用默认的 <code>MethodCodec</code> 对象即可。自定义 <code>MethodCodec</code> 的方式我们会在后文中介绍。</p><h3 id="方法调用"><a href="#方法调用" class="headerlink" title="方法调用"></a>方法调用</h3><p><code>MethodChannel</code> 提供了下列三种方法用于方法调用：</p><ul><li><code>Future&lt;T&gt; invokeMethod&lt;T&gt;(String method, [dynamic arguments])</code>：普通方法调用</li><li><code>Future&lt;List&lt;T&gt;&gt; invokeListMethod&lt;T&gt;(String method, [dynamic arguments])</code>：返回值是数组的方法调用</li><li><code>Future&lt;Map&lt;K, V&gt;&gt; invokeMapMethod&lt;K, V&gt;(String method, [dynamic arguments])</code>：返回值是 <code>Map</code> 的方法调用</li></ul><p>我们以 <code>invokeMethod</code> 方法为例：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;T&gt; invokeMethod&lt;T&gt;(<span class="built_in">String</span> method, [<span class="keyword">dynamic</span> arguments]) <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">assert</span>(method != <span class="keyword">null</span>);</span><br><span class="line">  <span class="keyword">final</span> ByteData result = <span class="keyword">await</span> BinaryMessages.send(</span><br><span class="line">    name,</span><br><span class="line">    codec.encodeMethodCall(MethodCall(method, arguments)),</span><br><span class="line">  );</span><br><span class="line">  <span class="keyword">if</span> (result == <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> MissingPluginException(<span class="string">'No implementation found for method <span class="subst">$method</span> on channel <span class="subst">$name</span>'</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">final</span> T typedResult = codec.decodeEnvelope(result);</span><br><span class="line">  <span class="keyword">return</span> typedResult;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在接收到方法调用的参数之后，首先会通过 <code>codec.encodeMethodCall(MethodCall(method, arguments))</code> 将方法名称和参数进行编码，然后再通过 <code>BinaryMessages.send</code> 将 <code>MethodChannel</code> 的 <code>name</code> 和编码之后的数据发送到对应的设备平台，并接收 <code>ByteData</code> 类型的返回值。</p><p>在接收到 <code>ByteData</code> 类型的返回值之后，如果返回值为 <code>null</code>，则抛出 <code>MissingPluginException</code>，视为设备平台上没有对应的方法。否则通过 <code>codec.decodeEnvelope(result)</code> 将返回值解码为具体的数据类型。我们以 <a href="https://github.com/flutter/plugins/tree/master/packages/path_provider" target="_blank" rel="noopener">path_provider/path_provider.dart</a> 项目代码为例：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;Result&gt; _getDirectory(<span class="built_in">String</span> methodName, [<span class="built_in">String</span> extra]) <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="built_in">String</span> path;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    path = <span class="keyword">await</span> _channel.invokeMethod(methodName, extra);</span><br><span class="line">    <span class="keyword">return</span> Result(Result.ERROR_CODE_SUCCESS, <span class="string">'success'</span>, Directory(<span class="string">'<span class="subst">$path</span>/'</span>));</span><br><span class="line">  &#125; on PlatformException <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="keyword">return</span> Result(<span class="built_in">num</span>.parse(e.code), e.message);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最终，我们通过如下代码完成一次 <code>Flutter</code> 层到设备平台的方法调用：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="built_in">String</span> _EXTERNAL_STORAGE_DIRECTORY_METHOD_NAME = <span class="string">"getExternalStorageDirectory"</span>;</span><br><span class="line">Future&lt;Result&gt; getExternalStorageDirectory() <span class="keyword">async</span></span><br><span class="line">  <span class="keyword">return</span> _getDirectory(_EXTERNAL_STORAGE_DIRECTORY_METHOD_NAME);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="响应来自设备平台的方法调用-setMethodCallHandler"><a href="#响应来自设备平台的方法调用-setMethodCallHandler" class="headerlink" title="响应来自设备平台的方法调用 setMethodCallHandler"></a>响应来自设备平台的方法调用 setMethodCallHandler</h3><p><code>setMethodCallHandler</code> 方法通过一个方法回调用来接收设备平台通过 <code>MethodChannel</code> 调用 <code>Flutter</code> 代码时的响应信息：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> setMethodCallHandler(Future&lt;<span class="keyword">dynamic</span>&gt; handler(MethodCall call)) &#123;</span><br><span class="line">  BinaryMessages.setMessageHandler(</span><br><span class="line">    name,</span><br><span class="line">    handler == <span class="keyword">null</span> ? <span class="keyword">null</span> : (ByteData message) =&gt; _handleAsMethodCall(message, handler),</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该方法最终是通过 <code>BinaryMessages.setMessageHandler</code> 将 <code>MethodChannel</code> 的 <code>name</code> 属性和 <code>Future&lt;ByteData&gt; handler(ByteData message)</code> 回调方法赋值给了 <code>BinaryMessages</code> 对象中的 <code>_handlers</code>。</p><p><code>_handleAsMethodCall</code> 方法则是对返回的结果进行处理：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;ByteData&gt; _handleAsMethodCall(ByteData message, Future&lt;<span class="keyword">dynamic</span>&gt; handler(MethodCall call)) <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">final</span> MethodCall call = codec.decodeMethodCall(message);</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> codec.encodeSuccessEnvelope(<span class="keyword">await</span> handler(call));</span><br><span class="line">  &#125; on PlatformException <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="keyword">return</span> codec.encodeErrorEnvelope(</span><br><span class="line">      code: e.code,</span><br><span class="line">      message: e.message,</span><br><span class="line">      details: e.details,</span><br><span class="line">    );</span><br><span class="line">  &#125; on MissingPluginException &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="keyword">return</span> codec.encodeErrorEnvelope(code: <span class="string">'error'</span>, message: e.toString(), details: <span class="keyword">null</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果方法调用成功，则通过 <code>MethodCodec</code> 的 <code>encodeSuccessEnvelope</code> 方法将结果编码为 <code>ByteData</code>，如果调用失败，则通过 <code>MethodCodec</code> 的 <code>encodeErrorEnvelope</code> 将错误码、错误信息和详情进行编码。我们以响应来自设备平台的一个方法调用（名称为 <code>a_method</code>）为例：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">_channel.setMethodCallHandler(_handler);</span><br><span class="line"></span><br><span class="line">Future&lt;<span class="keyword">dynamic</span>&gt; _handler(MethodCall methodCall) &#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="string">"a_method"</span> == methodCall.method) &#123;</span><br><span class="line">    <span class="built_in">print</span>(methodCall.method);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> Future.value(<span class="keyword">true</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过 <code>setMethodCallHandler</code> 设置方法调用的监听之后，我们可以根据 <code>methodCall.method</code> 判断具体的方法调用。</p><h2 id="Android-层的-MethodChannel"><a href="#Android-层的-MethodChannel" class="headerlink" title="Android 层的 MethodChannel"></a>Android 层的 MethodChannel</h2><p><code>Android</code> 平台上响应 <code>Flutter</code> 层的方法调用是通过 <code>MethodCallHandler</code> 实现的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MethodCallHandler</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onMethodCall</span><span class="params">(MethodCall methodCall, MethodChannel.Result result)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>设备平台端（<code>Android</code> 为例）响应 <code>Flutter</code> 层发送的方法调用的请求，需要实现 <code>MethodCallHandler</code> 接口用于接收方法调用的详细信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FlutterFilePathProvidePlugin</span> <span class="keyword">implements</span> <span class="title">MethodCallHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onMethodCall</span><span class="params">(MethodCall call, Result result)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>onMethodCall</code> 方法回调时会传入 <code>methodCall</code> 和 <code>result</code> 两个对象。其中，<code>methodCall</code> 包含了方法调用的基本信息（方法名称和参数）以及用于处理参数的几个接口。<code>result</code> 参数则用于在方法响应完毕之后，将返回值或者错误信息传递到 <code>Flutter</code> 层。上文中我们通过 <code>MethodChannel</code> 向设备平台发送了一个名称为 <code>getExternalStorageDirectory</code> 的方法调用，那么在 <code>Android</code> 平台上的具体响应代码为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String _EXTERNAL_STORAGE_DIRECTORY_METHOD_NAME = <span class="string">"getExternalStorageDirectory"</span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onMethodCall</span><span class="params">(MethodCall call, Result result)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (TextUtils.equals(call.method, _EXTERNAL_STORAGE_DIRECTORY_METHOD_NAME)) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            String path = getExternalStorageDirectory();</span><br><span class="line">            <span class="keyword">if</span> (TextUtils.isEmpty(path)) &#123;</span><br><span class="line">                result.error(<span class="string">"1"</span>, <span class="string">"path is null"</span>, <span class="string">""</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                result.success(path);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            result.error(<span class="string">"2"</span>, e.getMessage(), <span class="string">""</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> String <span class="title">getExternalStorageDirectory</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> Environment.getExternalStorageDirectory().getAbsolutePath();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述代码中，根据 <code>Flutter</code> 端传递过来的 <code>name</code> 属性，完成对应方法的调用，并通过 <code>result</code> 对象将结果回写回 <code>Flutter</code> 层：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Result</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">success</span><span class="params">(@Nullable Object var1)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">error</span><span class="params">(String var1, @Nullable String var2, @Nullable Object var3)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">notImplemented</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>与 <code>Flutter</code> 层相对应的，<code>Android</code> 端也需要初始化一个 <code>MethodChannel</code> 对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">registerWith</span><span class="params">(Registrar registrar)</span> </span>&#123;</span><br><span class="line">    FlutterFilePathProvidePlugin.registrar = registrar;</span><br><span class="line">    <span class="keyword">final</span> MethodChannel channel = <span class="keyword">new</span> MethodChannel(registrar.messenger(), _METHOD_CHANNEL_NAME);</span><br><span class="line">    channel.setMethodCallHandler(<span class="keyword">new</span> FlutterFilePathProvidePlugin());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="构造方法-1"><a href="#构造方法-1" class="headerlink" title="构造方法"></a>构造方法</h3><p><code>Android</code> 端的 <code>MethodChannel</code> 提供了两个构造方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">MethodChannel</span><span class="params">(BinaryMessenger messenger, String name)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(messenger, name, StandardMethodCodec.INSTANCE);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">MethodChannel</span><span class="params">(BinaryMessenger messenger, String name, MethodCodec codec)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">assert</span> messenger != <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">assert</span> name != <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">assert</span> codec != <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.messenger = messenger;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">    <span class="keyword">this</span>.codec = codec;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>与 <code>Flutter</code> 端相对应的，<code>Android</code> 端的 <code>MethodChannel</code> 也需要 <code>BinaryMessenger</code>、<code>name</code> 和 <code>MethodCodec</code> 三个对象。其中，<code>name</code> 属性需要与 <code>Flutter</code> 端初始化 <code>MethodChannel</code> 时传入的 <code>name</code> 保持一致。</p><h3 id="方法调用-1"><a href="#方法调用-1" class="headerlink" title="方法调用"></a>方法调用</h3><p><code>Android</code> 端的 <code>MethodChannel</code> 提供了下列两个方法用于调用 <code>Flutter</code> 层的方法：</p><ul><li><code>invokeMethod(@NonNull String method, @Nullable Object arguments)</code></li><li><code>invokeMethod(String method, @Nullable Object arguments, MethodChannel.Result callback)</code></li></ul><p>与 <code>Flutter</code> 层调用设备平台代码一样，<code>Android</code> 端调用 <code>Flutter</code> 层代码也需要先通过 <code>MethodCodec</code> 的 <code>encodeMethodCall</code> 方法将方法调用进行编码，然后通过 <code>BinaryMessenger.send</code> 将对应的信息发送到 <code>Flutter</code> 层。</p><h3 id="响应-Flutter-层的方法调用"><a href="#响应-Flutter-层的方法调用" class="headerlink" title="响应 Flutter 层的方法调用"></a>响应 Flutter 层的方法调用</h3><p><code>setMethodCallHandler(@Nullable MethodChannel.MethodCallHandler handler)</code> 方法通过接收一个 <code>handler</code> 对象来响应来自于 <code>Flutter</code> 层的方法调用。</p><p>与 <code>Flutter</code> 层调用设备平台的方法一样的，设备平台调用 <code>Flutter</code> 层的方法也是通过 <code>MethodChannel</code> 实现的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">channel.invokeMethod(<span class="string">"a_method"</span>, <span class="keyword">null</span>);</span><br></pre></td></tr></table></figure><h2 id="OptionalMethodChannel"><a href="#OptionalMethodChannel" class="headerlink" title="OptionalMethodChannel"></a>OptionalMethodChannel</h2><p><code>OptionalMethodChannel</code> 是 <code>MethodChannel</code> 的子类。与 <code>MethodChannel</code> 所不同的是，如果调用 <code>invokeMethod</code> 找不到与之对应的设备平台的方法，<code>MethodChannel</code> 以抛出异常的形式返回，而 <code>OptionalMethodChannel</code> 则是返回 <code>null</code>。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>MethodChannel</code> 的主要作用是提供一种 <code>Flutter</code> 端方法和设备平台端的方法相互调用的机制。默认情况下，<code>MethodChannel</code> 使用 <code>StandardMethodCodec</code> 作为数据的编解码器，并最终通过 <code>BinaryMessages</code> 完成与设备平台端数据的接收和传递。</p><p>在 <code>MethodChannel</code> 中除了默认的 <code>StandardMethodCodec</code> 作为 <code>codec</code> 的默认实现，还可以选择 <code>JSONMethodCodec</code> 作为 <code>codec</code> 编解码器的实现，用于传递 <code>JSON</code> 类型的数据。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在 &lt;code&gt;Flutter&lt;/code&gt; 中，&lt;code&gt;MethodChannel&lt;/code&gt; 允许我们发送与方法调用相关的消息。&lt;code&gt;MethodChannel&lt;/code&gt; 适用于 &lt;code&gt;Flutter&lt;/code&gt; 端和设备平台端进行直接的方法调用，这种方法调用是双向的，即既可以通过 &lt;code&gt;MethodChannel&lt;/code&gt; 从 &lt;code&gt;Flutter&lt;/code&gt; 调用设备平台上的方法，也可以从设备平台上调用 &lt;code&gt;Flutter&lt;/code&gt; 层的代码。&lt;br&gt;
    
    </summary>
    
      <category term="Flutter" scheme="https://fiissh.tech/categories/flutter/"/>
    
    
      <category term="MethodChannel" scheme="https://fiissh.tech/tags/methodchannel/"/>
    
  </entry>
  
  <entry>
    <title>Flutter 开发（11）：Flutter 层异常捕获</title>
    <link href="https://fiissh.tech/2019/flutter-exception-handler.html"/>
    <id>https://fiissh.tech/2019/flutter-exception-handler.html</id>
    <published>2019-03-25T06:23:29.000Z</published>
    <updated>2019-04-16T03:23:29.000Z</updated>
    
    <content type="html"><![CDATA[<p><code>Flutter</code> 中的异常可以分为同步异常和异步异常。通常情况下我们可以使用 <code>try-catch</code> 捕获同步异常，而异步异常则需要使用 <code>Flutter</code> 提供的其他 <code>API</code>。<br><a id="more"></a></p><h2 id="使用-try-catch-捕获异常"><a href="#使用-try-catch-捕获异常" class="headerlink" title="使用 try-catch 捕获异常"></a>使用 try-catch 捕获异常</h2><p>与 <code>Java</code> 语言类似的，<code>Dart</code> 中提供了 <code>try-catch</code> 代码块用于对同步异常的捕获：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;Result&gt; _getDirectory(<span class="built_in">String</span> methodName, [<span class="built_in">String</span> extra]) <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="built_in">String</span> path;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    path = <span class="keyword">await</span> _channel.invokeMethod(methodName, extra);</span><br><span class="line">    <span class="keyword">return</span> Result(Result.ERROR_CODE_SUCCESS, <span class="string">'success'</span>, Directory(<span class="string">'<span class="subst">$path</span>/'</span>));</span><br><span class="line">  &#125; on PlatformException <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="keyword">return</span> Result(<span class="built_in">num</span>.parse(e.code), e.message);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Flutter-框架层的异常捕获"><a href="#Flutter-框架层的异常捕获" class="headerlink" title="Flutter 框架层的异常捕获"></a>Flutter 框架层的异常捕获</h3><p><code>Flutter</code> 框架在很多关键位置自动进行了异常处理。例如，如果我们的 <code>Flutter</code> 页面出现错误，会自动弹出一个错误页面：</p><p><img src="https://user-gold-cdn.xitu.io/2019/1/16/1685691a02afb4b6?imageView2/0/w/1280/h/960/ignore-error/1" alt></p><p>这是由于系统在调用 <code>build()</code> 方法构建页面时进行了 <code>try-catch</code> 的处理，并提供一个 <code>ErrorWidget</code> 用于出现异常信息时调用的页面：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@override</span></span><br><span class="line"><span class="keyword">void</span> performRebuild() &#123;</span><br><span class="line"> ...</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">//执行build方法  </span></span><br><span class="line">    built = build();</span><br><span class="line">  &#125; <span class="keyword">catch</span> (e, stack) &#123;</span><br><span class="line">    <span class="comment">// 有异常时则弹出错误提示  </span></span><br><span class="line">    built = ErrorWidget.builder(_debugReportException(<span class="string">'building <span class="subst">$this</span>'</span>, e, stack));</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>_debugReportException</code> 方法中最终通过 <code>FlutterError.reportError</code> 将错误信息进行上报：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">FlutterErrorDetails _debugReportException(</span><br><span class="line">  <span class="built_in">String</span> context,</span><br><span class="line">  <span class="keyword">dynamic</span> exception,</span><br><span class="line">  StackTrace stack, &#123;</span><br><span class="line">  InformationCollector informationCollector</span><br><span class="line">&#125;) &#123;</span><br><span class="line">  <span class="comment">//构建错误详情对象  </span></span><br><span class="line">  <span class="keyword">final</span> FlutterErrorDetails details = FlutterErrorDetails(</span><br><span class="line">    exception: exception,</span><br><span class="line">    stack: stack,</span><br><span class="line">    <span class="keyword">library</span>: <span class="string">'widgets library'</span>,</span><br><span class="line">    context: context,</span><br><span class="line">    informationCollector: informationCollector,</span><br><span class="line">  );</span><br><span class="line">  <span class="comment">//报告错误</span></span><br><span class="line">  FlutterError.reportError(details);</span><br><span class="line">  <span class="keyword">return</span> details;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>reportError</code> 方法中最终通过 <code>onError</code> 属性处理错误信息（实际上 <code>onError</code> 有默认实现 <code>dumpErrorToConsole</code>）。我们可以通过重写 <code>onError</code> 属性拦截上报信息（<code>reportError</code> 方法）：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  FlutterError.onError = (FlutterErrorDetails details) &#123;</span><br><span class="line">    reportError(details);</span><br><span class="line">  &#125;;</span><br><span class="line">  runApp(FilePathProviderApplication());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果我们需要自定义错误页面，则需要重写 <code>ErrorWidget.builder</code>：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  ErrorWidget.builder = (FlutterErrorDetails flutterErrorDetails) &#123;</span><br><span class="line">    <span class="built_in">print</span>(flutterErrorDetails.toString());</span><br><span class="line">    <span class="keyword">return</span> Center(</span><br><span class="line">      child: Text(<span class="string">"Flutter 崩溃了"</span>),</span><br><span class="line">    );</span><br><span class="line">  &#125;;</span><br><span class="line">  runApp(FilePathProviderApplication());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="使用-Future-API-捕获异常"><a href="#使用-Future-API-捕获异常" class="headerlink" title="使用 Future API 捕获异常"></a>使用 Future API 捕获异常</h2><p><code>Future API</code> 提供了 <code>catchError</code> 方法用于捕获使用 <code>Future</code> 时抛出的异步异常：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">filePathProvide.getExternalStorageApplicationDirectory().then((value) &#123;</span><br><span class="line">  doSomething(value);</span><br><span class="line">  <span class="keyword">throw</span> (<span class="string">"exception message"</span>);</span><br><span class="line">&#125;).catchError(_handlerError);</span><br><span class="line"></span><br><span class="line">_handlerError() &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="使用-Zone-捕获未处理的异常"><a href="#使用-Zone-捕获未处理的异常" class="headerlink" title="使用 Zone 捕获未处理的异常"></a>使用 Zone 捕获未处理的异常</h2><p><code>Flutter</code> 提供了 <code>Zone.runZoned</code> 方法(<code>dart:async</code> 包)用于捕获所有未处理的异常信息：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">R runZoned&lt;R&gt;(R body(),&#123;<span class="built_in">Map</span> zoneValues, ZoneSpecification zoneSpecification, <span class="built_in">Function</span> onError&#125;)</span><br></pre></td></tr></table></figure><p>其中，<code>zoneValues</code> 是 <code>Zone</code> 的私有属性，<code>zoneSpecification</code> 是 <code>Zone</code> 的一些配置信息，可以自定义一些代码行为，<code>onError</code> 是 <code>Zone</code> 中未捕获异常的回调方法。</p><p>如果开发者提供了 <code>onError</code> 回调方法或者通过 <code>ZoneSpecification.handleUncaughtError</code> 指定了错误处理的回调方法，那么这个 <code>Zone</code> 将会变为一个 <code>error zone</code>，无论是同步环境还是异步环境，该 <code>error zone</code> 都会在出现未捕获的异常信息时调用：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  runZoned(() =&gt; runApp(FilePathProviderApplication()), onError: (<span class="built_in">Object</span> obj, StackTrace stack) &#123;</span><br><span class="line">    uploadException(<span class="string">"<span class="subst">$obj</span>\n<span class="subst">$stack</span>"</span>);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 <code>runZoned</code> 可以捕获 <code>Flutter</code> 层全局的未处理异常信息。我们可以结合 <code>FlutterError</code>，在非生产环境下将 <code>Flutter UI</code> 层的异常信息使用 <code>FlutterError</code> 进行处理，而生产环境下使用 <code>runZoned</code>进行处理：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="built_in">bool</span> inProduction = <span class="keyword">const</span> <span class="built_in">bool</span>.fromEnvironment(<span class="string">"dart.vm.product"</span>);</span><br><span class="line"></span><br><span class="line">FlutterError.onError = (FlutterErrorDetails details) <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (inProduction) &#123;</span><br><span class="line">    Zone.current.handleUncaughtError(details.exception, details.stack);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    FlutterError.dumpErrorToConsole(details);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line">runApp(FilePathProviderApplication());</span><br></pre></td></tr></table></figure><h2 id="Flutter-端异常捕获最佳实践"><a href="#Flutter-端异常捕获最佳实践" class="headerlink" title="Flutter 端异常捕获最佳实践"></a>Flutter 端异常捕获最佳实践</h2><p>对于 <code>Flutter</code> 端的异常信息，除了能够使用 <code>try-catch</code> 捕获的同步异常之外，大多是情况下异常信息集中在 <code>Flutter</code> 端的页面渲染方面，所以我们需要重写 <code>FlutterError.onError</code> 用于处理框架层自动添加的异常捕获：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="built_in">bool</span> inProduction = <span class="keyword">const</span> <span class="built_in">bool</span>.fromEnvironment(<span class="string">"dart.vm.product"</span>);</span><br><span class="line"></span><br><span class="line">FlutterError.onError = (FlutterErrorDetails details) <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (inProduction) &#123;</span><br><span class="line">    Zone.current.handleUncaughtError(details.exception, details.stack);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    FlutterError.dumpErrorToConsole(details);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在非生产环境下，异常信息通过 <code>FlutterError.dumpErrorToConsole(details)</code> 交由控制台进行输出，生产环境则通过 <code>Zone.current.handleUncaughtError(details.exception, details.stack)</code> 将异常信息交付给 <code>Flutter</code> 层的全局异常捕获进行处理。</p><p>然后，我们再为整个应用程序的 <code>Flutter</code> 层添加全局异常捕获：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">runZoned&lt;Future&lt;<span class="built_in">Null</span>&gt;&gt;(() <span class="keyword">async</span> &#123;</span><br><span class="line">    runApp(<span class="keyword">new</span> HomeApp());</span><br><span class="line">  &#125;, onError: (error, stackTrace) <span class="keyword">async</span> &#123;</span><br><span class="line">    <span class="keyword">await</span> _reportError(error, stackTrace);</span><br><span class="line">  &#125;);</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;code&gt;Flutter&lt;/code&gt; 中的异常可以分为同步异常和异步异常。通常情况下我们可以使用 &lt;code&gt;try-catch&lt;/code&gt; 捕获同步异常，而异步异常则需要使用 &lt;code&gt;Flutter&lt;/code&gt; 提供的其他 &lt;code&gt;API&lt;/code&gt;。&lt;br&gt;
    
    </summary>
    
      <category term="Flutter" scheme="https://fiissh.tech/categories/flutter/"/>
    
    
      <category term="Flutter 异常捕获" scheme="https://fiissh.tech/tags/flutter-%E5%BC%82%E5%B8%B8%E6%8D%95%E8%8E%B7/"/>
    
  </entry>
  
  <entry>
    <title>Mac 下使用 Docker 安装 TensorFlow</title>
    <link href="https://fiissh.tech/2019/install-tensorflow-by-docker.html"/>
    <id>https://fiissh.tech/2019/install-tensorflow-by-docker.html</id>
    <published>2019-03-22T09:04:25.000Z</published>
    <updated>2019-03-22T09:04:25.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文是 <code>TensorFlow</code> 官方文档的随手记录，详细信息还请移步 <a href="https://www.tensorflow.org/install/docker?hl=zh-cn" target="_blank" rel="noopener">Docker</a>。</p><p>官方 <code>TensorFlow Docker</code> 映像位于 <a href="https://hub.docker.com/r/tensorflow/tensorflow/" target="_blank" rel="noopener">tensorflow/tensorflow Docker Hub</a> 代码库中。映像版本按照以下格式进行标记：</p><table><thead><tr><th style="text-align:center">标记</th><th style="text-align:center">说明</th></tr></thead><tbody><tr><td style="text-align:center">latest</td><td style="text-align:center">TensorFlow CPU 二进制映像的最新版本</td></tr><tr><td style="text-align:center">nightly</td><td style="text-align:center">TensorFlow 映像的每夜版</td></tr><tr><td style="text-align:center">version</td><td style="text-align:center">指定 TensorFlow 二进制映像的版本</td></tr><tr><td style="text-align:center">tag-devel</td><td style="text-align:center">指定的标记版本和源代码。</td></tr><tr><td style="text-align:center">tag-gpu</td><td style="text-align:center">支持 GPU 的指定标记版本</td></tr><tr><td style="text-align:center">tag-py3</td><td style="text-align:center">支持 Python 3 的指定标记版本</td></tr><tr><td style="text-align:center">tag-gpu-py3</td><td style="text-align:center">支持 GPU 和 Python 3 的指定标记版本</td></tr><tr><td style="text-align:center">tag-devel-py3</td><td style="text-align:center">支持 Python 3 的指定标记版本以及源代码</td></tr><tr><td style="text-align:center">tag-devel-gpu</td><td style="text-align:center">支持 GPU 的指定标记版本以及源代码</td></tr><tr><td style="text-align:center">tag-devel-gpu-py3</td><td style="text-align:center">支持 GPU 和 Python 3 的指定标记版本以及源代码</td></tr></tbody></table><p>使用如下命令安装：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker pull tensorflow/tensorflow</span><br></pre></td></tr></table></figure><p>或者指定版本安装：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker pull tensorflow/tensorflow:nightly-devel-gpu</span><br></pre></td></tr></table></figure><p>使用如下命令启动 <code>TensorFlow Docker</code> 容器：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run [-it] [--rm] [-p hostPort:containerPort] tensorflow/tensorflow[:tag] [command]</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run --name my-tensorflow -it -p 8888:8888 -v ~/tensorflow:/test/data tensorflow/tensorflow</span><br></pre></td></tr></table></figure><ul><li><code>-name</code>：创建的容器名，即 <code>my-tensorflow</code></li><li><code>-it</code>：保留命令行运行</li><li><code>-p 8888:8888</code>：将本地的8888端口和 <code>http://localhost:8888/</code> 映射</li><li><code>-v ~/tensorflow:/test/data</code>:将本地的 <code>~/tensorflow</code> 挂载到容器内的 <code>/test/data</code> 下</li><li><code>tensorflow/tensorflow</code>：默认是 <code>tensorflow/tensorflow:latest</code>，指定使用的镜像</li></ul><p>输入以上命令后，默认容器就被启动了，命令行显示：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">[I 15:08:31.949 NotebookApp] Writing notebook server cookie secret to /root/.local/share/jupyter/runtime/notebook_cookie_secret</span><br><span class="line">[W 15:08:31.970 NotebookApp] WARNING: The notebook server is listening on all IP addresses and not using encryption. This is not recommended.</span><br><span class="line">[I 15:08:31.975 NotebookApp] Serving notebooks from local directory: /notebooks</span><br><span class="line">[I 15:08:31.975 NotebookApp] 0 active kernels</span><br><span class="line">[I 15:08:31.975 NotebookApp] The Jupyter Notebook is running at:</span><br><span class="line">[I 15:08:31.975 NotebookApp] http://[all ip addresses on your system]:8888/?token=649d7cab1734e01db75b6c2b476ea87aa0b24dde56662a27</span><br><span class="line">[I 15:08:31.975 NotebookApp] Use Control-C to stop this server and shut down all kernels (twice to skip confirmation).</span><br><span class="line">[C 15:08:31.975 NotebookApp]</span><br><span class="line"></span><br><span class="line">    Copy/paste this URL into your browser when you connect for the first time,</span><br><span class="line">    to login with a token:</span><br><span class="line">        ；</span><br><span class="line">[I 15:09:08.581 NotebookApp] 302 GET /?token=649d7cab1734e01db75b6c2b476ea87aa0b24dde56662a27 (172.17.0.1) 0.42ms</span><br></pre></td></tr></table></figure><p>拷贝带 <code>token</code> 的 <code>URL</code> 在浏览器打开:</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http://[p addresses on your system]:8888/?token=649d7cab1734e01db75b6c2b476ea87aa0b24dde56662a27</span><br></pre></td></tr></table></figure><p>你将会看到如下页面：</p><p><img src="https://user-gold-cdn.xitu.io/2018/2/24/161c694e4193e2b7?imageView2/0/w/1280/h/960/ignore-error/1" alt></p><p>使用如下命令关闭容器：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker stop my-tensortflow</span><br></pre></td></tr></table></figure><p>再次打开时可以使用如下命令：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker start my-tensortflow</span><br></pre></td></tr></table></figure><p>或者使用如下命令创建基于命令行的容器：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -it --name bash_tensorflow tensorflow/tensorflow /bin/bash</span><br></pre></td></tr></table></figure><p>使用 <code>start</code> 命令启动容器：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker start bash_tensorflow</span><br></pre></td></tr></table></figure><p>再连接上容器：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker attach bash_tensorflow</span><br></pre></td></tr></table></figure><p>关于 <code>Your CPU supports instructions that this TensorFlow binary was not compiled to use: AVX AVX2</code> 错误的解决方案：</p><ul><li>如果安装的是 <code>CPU</code> 版本，在代码中加入如下代码：</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line">os.environ[<span class="string">'TF_CPP_MIN_LOG_LEVEL'</span>] = <span class="string">'2'</span></span><br></pre></td></tr></table></figure><ul><li>如果安装的是 <code>GPU</code> 版本，在代码中加入如下代码：</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line">os.environ[<span class="string">'TF_CPP_MIN_LOG_LEVEL'</span>] = <span class="string">'2'</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;本文是 &lt;code&gt;TensorFlow&lt;/code&gt; 官方文档的随手记录，详细信息还请移步 &lt;a href=&quot;https://www.tensorflow.org/install/docker?hl=zh-cn&quot; target=&quot;_blank&quot; rel=&quot;noopener
      
    
    </summary>
    
      <category term="TensorFlow" scheme="https://fiissh.tech/categories/tensorflow/"/>
    
    
      <category term="安装 TensorFlow" scheme="https://fiissh.tech/tags/%E5%AE%89%E8%A3%85-tensorflow/"/>
    
      <category term="Docker" scheme="https://fiissh.tech/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>IllegalArgumentException Service Intent Must Be Explicit</title>
    <link href="https://fiissh.tech/2019/illegalargument-exception-service-intent-must-be-explicit.html"/>
    <id>https://fiissh.tech/2019/illegalargument-exception-service-intent-must-be-explicit.html</id>
    <published>2019-03-21T09:21:54.000Z</published>
    <updated>2019-03-21T09:21:54.000Z</updated>
    
    <content type="html"><![CDATA[<p>在 <code>AndroidMenifest</code> 文件中注册一个 <code>Service</code>，并设置其 <code>action</code>属性：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">service</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">".MathService"</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:process</span>=<span class="string">".math"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">"tech.fiissh.sample.math"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">service</span>&gt;</span></span><br></pre></td></tr></table></figure><p>然后使用 <code>Intent</code> 绑定该 <code>Service</code> 时抛出异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Intent intent = <span class="keyword">new</span> Intent(<span class="string">"tech.fiissh.sample.math"</span>)</span><br><span class="line">bindService(intent, connection, Context.BIND_AUTO_CREATE);</span><br></pre></td></tr></table></figure><p>异常信息：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Caused by: java.lang.IllegalArgumentException: Service Intent must be explicit: Intent</span><br><span class="line">   at android.app.ContextImpl.validateServiceIntent(ContextImpl.java:1464)</span><br><span class="line">   at android.app.ContextImpl.bindServiceCommon(ContextImpl.java:1609)</span><br><span class="line">   at android.app.ContextImpl.bindService(ContextImpl.java:1557)</span><br><span class="line">   at android.content.ContextWrapper.bindService(ContextWrapper.java:684)</span><br></pre></td></tr></table></figure><p>造成该问题的主要原因是从 <code>Lollipop</code> 开始，启动 <code>Service</code> 必须使用显式 <code>Intent</code>。解决该问题的方案是为 <code>Intent</code> 添加 <code>package</code> 属性：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Intent intent = <span class="keyword">new</span> Intent(<span class="string">"tech.fiissh.sample.math"</span>);</span><br><span class="line">intent.setPackage(getPackageName());</span><br><span class="line">bindService(intent, connection, Context.BIND_AUTO_CREATE);</span><br></pre></td></tr></table></figure><p>更为详细的讨论请移步 <a href="https://stackoverflow.com/questions/24480069/google-in-app-billing-illegalargumentexception-service-intent-must-be-explicit/26318757#26318757" target="_blank" rel="noopener">IllegalArgumentException: Service Intent must be explicit, after upgrading to Android L Dev Preview</a>。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;在 &lt;code&gt;AndroidMenifest&lt;/code&gt; 文件中注册一个 &lt;code&gt;Service&lt;/code&gt;，并设置其 &lt;code&gt;action&lt;/code&gt;属性：&lt;/p&gt;
&lt;figure class=&quot;highlight xml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td 
      
    
    </summary>
    
      <category term="Android Exception" scheme="https://fiissh.tech/categories/android-exception/"/>
    
    
      <category term="IllegalArgumentException" scheme="https://fiissh.tech/tags/illegalargumentexception/"/>
    
      <category term="explicit Intent" scheme="https://fiissh.tech/tags/explicit-intent/"/>
    
  </entry>
  
  <entry>
    <title>CMake Was Unable to Find a Build Program Corresponding to Ninja 错误</title>
    <link href="https://fiissh.tech/2019/cmake-error-cmake-was-unable-to-find-build-program-corresponding-to-ninja.html"/>
    <id>https://fiissh.tech/2019/cmake-error-cmake-was-unable-to-find-build-program-corresponding-to-ninja.html</id>
    <published>2019-03-19T10:09:58.000Z</published>
    <updated>2019-03-19T10:09:58.000Z</updated>
    
    <content type="html"><![CDATA[<p><code>Android Studio</code> 升级之后新建项目（包含 <code>NDK</code>）之后编译报错：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CMake Error: CMake was unable to find a build program corresponding to Ninja.  CMAKE_MAKE_PROGRAM is not set.  You probably need to select a different build tool.</span><br></pre></td></tr></table></figure><p>造成该问题的原因是系统没有安装 <code>Ninja</code>。使用 <code>brew</code> 安装 <code>Ninja</code> 即可解决该问题：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install ninja</span><br></pre></td></tr></table></figure><p><a href="https://stackoverflow.com/questions/38658014/ninja-not-found-by-cmake" target="_blank" rel="noopener">StackOverflow-Ninja not found by CMake</a>对于该问题的解决方案是使用 <code>ln -s /usr/bin/ninja /usr/bin/ninja-build</code> 修改 <code>Ninja</code> 的引用。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;code&gt;Android Studio&lt;/code&gt; 升级之后新建项目（包含 &lt;code&gt;NDK&lt;/code&gt;）之后编译报错：&lt;/p&gt;
&lt;figure class=&quot;highlight shell&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;
      
    
    </summary>
    
      <category term="Android Exception" scheme="https://fiissh.tech/categories/android-exception/"/>
    
    
      <category term="CMake Error" scheme="https://fiissh.tech/tags/cmake-error/"/>
    
      <category term="Ninja" scheme="https://fiissh.tech/tags/ninja/"/>
    
  </entry>
  
  <entry>
    <title>Flutter 开发（10）：Flutter 的运行模式</title>
    <link href="https://fiissh.tech/2019/flutter-running-mode.html"/>
    <id>https://fiissh.tech/2019/flutter-running-mode.html</id>
    <published>2019-03-14T03:26:40.000Z</published>
    <updated>2019-03-14T03:26:40.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要是对 <code>Flutter</code> 的基本框架进行介绍。本文主要参考了 <a href="https://tech.meituan.com/2018/08/09/waimai-flutter-practice.html" target="_blank" rel="noopener">美团技术团队-Flutter 原理与实践</a> 一文。<a href="https://tech.meituan.com/2018/08/09/waimai-flutter-practice.html" target="_blank" rel="noopener">美团技术团队-Flutter 原理与实践</a> 是一篇非常优秀的介绍 <code>Flutter</code> 原理的文章。</p><p><code>Flutter</code> 的设计目标是高性能的跨平台的（<code>Android</code> 和 <code>iOS</code>）<code>UI</code> 框架。与传统的基于 <code>WebView</code> 的（例如 <code>Cordova</code> 或者 <code>AppCan</code> 等框架）或者基于原生控件系统的（例如 <code>React Native</code>）跨平台开发框架所不同的是，<code>Flutter</code> 是基于跨平台的渲染引擎 <code>Skia</code> 来实现的全新的跨平台的开发框架。这也就是说，<code>Flutter</code> 对于设备平台的依赖只有图形渲染相关的部分，这在一定程度上保证了不同平台、不同设备上的一致的用户体验。从执行效率的角度来说，<code>Flutter</code> 使用的 <code>Dart</code> 语言相较于其他大多数跨平台开发框架使用的 <code>JavaScript</code> 语言在执行效率上高效很多。<br><a id="more"></a></p><h2 id="Flutter-架构"><a href="#Flutter-架构" class="headerlink" title="Flutter 架构"></a>Flutter 架构</h2><p><code>Flutter</code> 的架构主要由三部分组成：</p><ul><li><code>Framework</code> 层</li><li><code>Engine</code> 层</li><li><code>Embedder</code> 层</li></ul><p><img src="http://www.techug.com/wordpress/wp-content/uploads/2018/07/66372-20180706175503147-184515056.jpg" alt="Flutter 架构"></p><h3 id="Framework-层"><a href="#Framework-层" class="headerlink" title="Framework 层"></a>Framework 层</h3><p><code>Flutter</code> 的 <code>Framework</code> 层使用 <code>Dart</code> 语言实现，包括了 <code>Material Design</code> （针对于 <code>Android</code> 平台）和 <code>Cupertino</code> （对于 <code>iOS</code> 平台）两种风格的文本、图片、按钮等基础 <code>Widget</code>、以及动画、手势处理和渲染引擎等模块。这一部分的源代码对应的是 <code>Flutter</code> 仓库下的 <a href="https://github.com/flutter/flutter/tree/master/packages" target="_blank" rel="noopener">flutter/packages/</a> <code>Flutter</code> 包以及 <a href="https://github.com/flutter/engine/tree/master/sky/packages/sky_engine" target="_blank" rel="noopener">engine/sky/packages/sky_engine/</a> 仓库下的 <code>io</code>、<code>async</code> 和 <code>ui</code> 等 <code>package</code>。</p><p><img src="https://awps-assets.meituan.net/mit-x/blog-images-bundle-2018a/9924fbed.png" alt></p><p><code>Flutter</code> 提供了非常丰富的控件，通过对不同类型的控件的嵌套和组合，我们可以方便的构建出任意功能、任意复杂度的界面。与其他将控件、控制器、布局以及其他属性分离开的框架不同的是，<code>Flutter</code> 提供了统一的对象模型：<code>Widget</code>。</p><p><code>Widget</code> 本身由许多更小的、功能单一的 <code>Widget</code> 组成。</p><h3 id="Engine-层"><a href="#Engine-层" class="headerlink" title="Engine 层"></a>Engine 层</h3><p><code>Flutter</code> 的 <code>Engine</code> 层主要包含 <code>Skia</code>、<code>Dart</code> 和 <code>Text</code> 三部分。<code>Engine</code> 主要使用 <code>C/C++</code> 开发。</p><p><code>Skia</code> 是开源的二维图形渲染库，提供了适用于多种平台的通用 <code>API</code>。</p><p><code>Dart</code> 主要包括 <code>Dart Runtime</code>、<code>Garbage Collection</code>，如果是 <code>Debug</code> 模式的话，该部分还提供了对 <code>JIT</code>（<code>Just In Time</code>）的支持。<code>Release</code> 和 <code>Profile</code> 模式下，是通过 <code>AOT</code>（<code>Ahead of Time</code>）编译成了原生代码，并不包含 <code>JIT</code> 部分。</p><p><code>Text</code> 是文本渲染库。<code>Android</code> 上使用 <code>FreeType</code> 渲染，<code>iOS</code>上使用 <code>CoreGraphics</code> 进行渲染。</p><h3 id="Embedder-层"><a href="#Embedder-层" class="headerlink" title="Embedder 层"></a>Embedder 层</h3><p><code>Embedder</code> 层主要包含 <code>Surface</code>、线程以及插件等，是 <code>Flutter</code> 的嵌入层，主要负责把 <code>Flutter</code> 嵌入到各个平台上。</p><h2 id="Flutter-的运行模式"><a href="#Flutter-的运行模式" class="headerlink" title="Flutter 的运行模式"></a>Flutter 的运行模式</h2><p><code>Flutter</code> 提供了四种运行模式：</p><ul><li><code>Debug</code> 模式</li><li><code>Release</code> 模式</li><li><code>Profile</code> 模式</li><li><code>Test</code> 模式</li></ul><p>上述四种模式在 <code>Flutter</code> 编译时是完全独立的。</p><h3 id="Debug-模式"><a href="#Debug-模式" class="headerlink" title="Debug 模式"></a>Debug 模式</h3><p><code>Debug</code> 模式可以在真机和模拟器上运行。该模式会打开所有的断言（包括 <code>debugging</code> 信息、<code>debugger aida</code> 和服务扩展），优化了快速 <code>develop/run</code> 循环，但是没有优化执行速度、二进制文件和部署。<code>Android Studio</code> 的 <code>run</code> 命令或者 <code>flutter run</code> 命名都是以该模式运行应用程序的。对应的编译命令为 <code>sky/tools/gn --android</code> 或者  <code>sky/tools/gn --ios</code>。</p><h3 id="Release-模式"><a href="#Release-模式" class="headerlink" title="Release 模式"></a>Release 模式</h3><p><code>Release</code> 模式只能在真机中运行，不能在模拟器中运行。该模式会关闭所有的断言（包括 <code>debugging</code> 信息、<code>debugger aida</code> 和服务扩展），优化启动速度并减少包体积。该模式是为了部署应用程序给最终的用户使用的。使用 <code>flutter run --release</code> 命令可以让应用程序在该模式下运行。对应的编译命令为 <code>sky/tools/gn --android --runtime-mode=release</code> 或者 <code>sky/tools/gn --ios --runtime-mode=release</code>。</p><h3 id="Profile-模式"><a href="#Profile-模式" class="headerlink" title="Profile 模式"></a>Profile 模式</h3><p><code>Profile</code> 模式只能在真机上运行，不能在模拟器中运行。该模式除了启用了服务扩展和 <code>tracing</code>（包含一些最低限度支持 <code>tracing</code> 运行的功能），其他方面和 <code>Release</code> 模式是一样的。使用 <code>flutter run --profile</code> 命令可以应用程序在该模式下运行。对应的编译命令为 <code>sky/tools/gn --android --runtime-mode=profile</code> 或者 <code>sky/tools/gn --ios --runtime-mode=profile</code>。</p><h3 id="Test-模式"><a href="#Test-模式" class="headerlink" title="Test 模式"></a>Test 模式</h3><p><code>Test</code> 模式只能在桌面上运行，与 <code>Debug</code> 模式基本上一样。</p><h2 id="Flutter-应用程序运行机制"><a href="#Flutter-应用程序运行机制" class="headerlink" title="Flutter 应用程序运行机制"></a>Flutter 应用程序运行机制</h2><p>在 <code>Android</code> 系统上，<code>Flutter</code> 构建出的 <code>APK</code> 在运行时会将所有 <code>assets</code> 目录下的资源文件解压到应用程序私有文件目录中的 <code>flutter</code> 目录下，主要包括处理字符编码的 <code>icudtl.dat</code>，以及 <code>Debug</code> 模式的 <code>kernel_blob.bin</code>、<code>platform.dill</code> 和 <code>Release</code> 模式下的4个 <code>snapshot</code> 文件。</p><p>默认情况下，<code>Flutter</code> 在 <code>Application</code> 的 <code>onCreate</code> 时调用 <code>FlutterMain</code> 的 <code>startInitialization</code> 方法来启动解压任务，然后在 <code>FlutterActivityDelegate</code> 的 <code>onCreate</code> 中调用 <code>FlutterMain</code> 的 <code>ensureInitializationComplete</code> 等待解压任务结束。</p><p><code>Flutter</code> 在 <code>Debug</code> 模式下使用 <code>JIT</code> 编译方式，主要是为了支持热加载功能：</p><p><img src="https://p1.meituan.net/travelcube/89aa7ce412124d5618877589b07f115426247.png" alt></p><p>触发热加载时 <code>Flutter</code> 会检测发生改变的 <code>Dart</code> 文件，将其同步到应用程序私有缓存目录下，<code>Dart VM</code> 加载并且修改对应的类或者方法，重建控件树后立即就可以在设备上看到效果。</p><p>在 <code>Release</code> 模式下，<code>Flutter</code> 会直接将 <code>snapshot</code> 文件映射到内存中执行其中的指令：</p><p><img src="https://p0.meituan.net/travelcube/ef7a282f280957a1a67020a2aa7922f524600.png" alt></p><p>在 <code>Release</code> 模式下，<code>FlutterActivityDelegate</code> 的 <code>onCreate</code> 中调用 <code>FlutterMain</code> 的 <code>ensureInitializationComplete</code> 方法时会将 <code>AndroidManifest</code> 中设置的 <code>snapshot</code> 文件名等运行参数设置到对应的 <code>C/C++</code> 同名类对象中，构造 <code>FlutterNativeView</code> 实例时调用 <code>nativeAttach</code> 来初始化 <code>Dart VM</code>，最后运行编译好的 <code>Dart</code> 代码。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文主要是对 &lt;code&gt;Flutter&lt;/code&gt; 的基本框架进行介绍。本文主要参考了 &lt;a href=&quot;https://tech.meituan.com/2018/08/09/waimai-flutter-practice.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;美团技术团队-Flutter 原理与实践&lt;/a&gt; 一文。&lt;a href=&quot;https://tech.meituan.com/2018/08/09/waimai-flutter-practice.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;美团技术团队-Flutter 原理与实践&lt;/a&gt; 是一篇非常优秀的介绍 &lt;code&gt;Flutter&lt;/code&gt; 原理的文章。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;Flutter&lt;/code&gt; 的设计目标是高性能的跨平台的（&lt;code&gt;Android&lt;/code&gt; 和 &lt;code&gt;iOS&lt;/code&gt;）&lt;code&gt;UI&lt;/code&gt; 框架。与传统的基于 &lt;code&gt;WebView&lt;/code&gt; 的（例如 &lt;code&gt;Cordova&lt;/code&gt; 或者 &lt;code&gt;AppCan&lt;/code&gt; 等框架）或者基于原生控件系统的（例如 &lt;code&gt;React Native&lt;/code&gt;）跨平台开发框架所不同的是，&lt;code&gt;Flutter&lt;/code&gt; 是基于跨平台的渲染引擎 &lt;code&gt;Skia&lt;/code&gt; 来实现的全新的跨平台的开发框架。这也就是说，&lt;code&gt;Flutter&lt;/code&gt; 对于设备平台的依赖只有图形渲染相关的部分，这在一定程度上保证了不同平台、不同设备上的一致的用户体验。从执行效率的角度来说，&lt;code&gt;Flutter&lt;/code&gt; 使用的 &lt;code&gt;Dart&lt;/code&gt; 语言相较于其他大多数跨平台开发框架使用的 &lt;code&gt;JavaScript&lt;/code&gt; 语言在执行效率上高效很多。&lt;br&gt;
    
    </summary>
    
      <category term="Flutter" scheme="https://fiissh.tech/categories/flutter/"/>
    
    
      <category term="Flutter 运行模式" scheme="https://fiissh.tech/tags/flutter-%E8%BF%90%E8%A1%8C%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>孙子兵法：用间第十三</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-13.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-13.html</id>
    <published>2019-03-12T10:52:27.000Z</published>
    <updated>2019-03-12T10:52:27.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>凡兴师十万，出征千里，百姓之费，公家之奉，日费千金，内外骚动，怠于道路，不得操事者，七十万家。相守数年，以争一日之胜，而爱爵禄百金，不知敌之情者，不仁之至也，非民之将也，非主之佐也，非胜之主也。故明君贤将所以动而胜人，成功出于众者，先知也。先知者，不可取于鬼神，不可象于事，不可验于度，必取于人，知敌之情者也。</p><p>故用间有五：有因间，有内间，有反间，有死间，有生间。五间俱起，莫知其道，是谓神纪，人君之宝也。乡间者，因其乡人而用之；内间者，因其官人而用之；反间者，因其敌间而用之；死间者，为诳事于外，令吾闻知之而传于敌间也；生间者，反报也。故三军之事，莫亲于间，赏莫厚于间，事莫密于间，非圣贤不能用间，非仁义不能使间，非微妙不能得间之实。微哉微哉！无所不用间也。间事未发而先闻者，间与所告者兼死。凡军之所欲击，城之所欲攻，人之所欲杀，必先知其守将、左右、谒者、门者、舍人之姓名，令吾间必索知之。敌间之来间我者，因而利之，导而舍之，故反间可得而用也；因是而知之，故乡间、内间可得而使也；因是而知之，故死间为诳事，可使告敌；因是而知之，故生间可使如期。五间之事，主必知之，知之必在于反间，故反间不可不厚也。</p><p>昔殷之兴也，伊挚在夏；周之兴也，吕牙在殷。故明君贤将，能以上智为间者，必成大功。此兵之要，三军之所恃而动也。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;凡兴师十万，出征千里，百姓之费，公家之奉，日费千金，内外骚动，怠于道路，不得操事者，七十万家。相守数年，以争一日之胜，而爱爵禄百金，不知敌之情者，不仁之至也，非民之将也，非主之佐也，非胜之主也。故明君贤将所以动而胜人，成功出于众者，先知也。先知者，不
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：火攻第十二</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-12.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-12.html</id>
    <published>2019-03-12T10:52:26.000Z</published>
    <updated>2019-03-12T10:52:26.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>凡火攻有五：一曰火人，二曰火积，三曰火辎，四曰火库，五曰火队。</p><p>行火必有因，因必素具。发火有时，起火有日。时者，天之燥也。日者，月在箕、壁、翼、轸也。凡此四宿者，风起之日也。凡火攻，必因五火之变而应之：火发于内，则早应之于外；火发而其兵静者，待而勿攻，极其火力，可从而从之，不可从则上。火可发于外，无待于内，以时发之，火发上风，无攻下风，昼风久，夜风止。凡军必知五火之变，以数守之。</p><p>故以火佐攻者明，以水佐攻者强。水可以绝，不可以夺。</p><p>夫战胜攻取而不惰其功者凶，命曰”费留”。故曰：明主虑之，良将惰之，非利不动，非得不用，非危不战。主不可以怒而兴师，将不可以愠而攻战。合于利而动，不合于利而上。怒可以复喜，愠可以复说，亡国不可以复存，死者不可以复生。故明主慎之，良将警之。此安国全军之道也。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;凡火攻有五：一曰火人，二曰火积，三曰火辎，四曰火库，五曰火队。&lt;/p&gt;
&lt;p&gt;行火必有因，因必素具。发火有时，起火有日。时者，天之燥也。日者，月在箕、壁、翼、轸也。凡此四宿者，风起之日也。凡火攻，必因五火之变而应之：火发于内，则早应之于外；火发而其兵
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：九地第十一</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-11.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-11.html</id>
    <published>2019-03-12T10:52:25.000Z</published>
    <updated>2019-03-12T10:52:25.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>用兵之法，有散地，有轻地，有争地，有交地，有衢地，有重地，有泛地，有围地，有死地。诸侯自战其地者，为散地；入人之地不深者，为轻地。我得则利，彼得亦利者，为争地；我可以往，彼可以来者，为交地；诸侯之地三属，先至而得天下众者，为衢地；入人之地深，背城邑多者，为重地；山林、险阻、沮泽，凡难行之道者，为泛地；所由入者隘，所从归者迂，彼寡可以击吾之众者，为围地；疾战则存，不疾战则亡者，为死地。是故散地则无战，轻地则无止，争地则无攻，交地则无绝，衢地则合交，重地则掠，泛地则行，围地则谋，死地则战。</p><p>古之善用兵者，能使敌人前后不相及，众寡不相恃，贵贱不相救，上下不相收，卒离而不集，兵合而不齐。合于利而动，不合于利而止。敢问敌所爱则听矣。兵之情主速，乘人之不及。由不虞之道，攻其所不戒也。</p><p>凡为客之道，深入则专。主人不克，掠于饶野，三军足食。谨养而勿劳，并气积力，运兵计谋，为不可测。</p><p>投之无所往，死且不北。死焉不得，士人尽力。兵士甚陷则不惧，无所往则固，深入则拘，不得已则斗。是故其兵不修而戒，不求而得，不约而亲，不令而信，禁祥去疑，至死无所之。</p><p>吾士无余财，非恶货也；无余命，非恶寿也。令发之日，士卒坐者涕沾襟，偃卧者涕交颐，投之无所往，诸、刿之勇也。故善用兵者，譬如率然。率然者，常山之蛇也。击其首则尾至，击其尾则首至，击其中则首尾俱至。敢问兵可使如率然乎？曰可。夫吴人与越人相恶也，当其同舟而济而遇风，其相救也如左右手。是故方马埋轮，未足恃也；齐勇如一，政之道也；刚柔皆得，地之理也。故善用兵者，携手若使一人，不得已也。</p><p>将军之事，静以幽，正以治，能愚士卒之耳目，使之无知；易其事，革其谋，使人无识；易其居，迂其途，使民不得虑。帅与之期，如登高而去其梯；帅与之深入诸侯之地，而发其机。若驱群羊，驱而往，驱而来，莫知所之。聚三军之众，投之于险，此谓将军之事也。</p><p>九地之变，屈伸之力，人情之理，不可不察也。</p><p>凡为客之道，深则专，浅则散。去国越境而师者，绝地也；四彻者，衢地也；入深者，重地也；入浅者，轻地也；背固前隘者，围地也；无所往者，死地也。</p><p>是故散地吾将一其志，轻地吾将使之属，争地吾将趋其后，交地吾将谨其守，交地吾将 固其结，衢地吾将谨其恃，重地吾将继其食，泛地吾将进其途，围地吾将塞其阙，死地吾将 示之以不活。</p><p>故兵之情：围则御，不得已则斗，过则从。</p><p>是故不知诸侯之谋者，不能预交；不知山林、险阻、沮泽之形者，不能行军；不用乡 导，不能得地利。四五者，一不知，非霸王之兵也。夫霸王之兵，伐大国，则其众不得聚； 威加于敌，则其交不得合。是故不争天下之交，不养天下之权，信己之私，威加于敌，则其 城可拔，其国可隳。</p><p>施无法之赏，悬无政之令。犯三军之众，若使一人。犯之以事，勿告以言；犯之以害，勿告以利。投之亡地然后存，陷之死地然后生。夫众陷于害，然后能为胜败。</p><p>故为兵之事，在顺详敌之意，并敌一向，千里杀将，是谓巧能成事。是故政举之日，夷关折符，无通其使，厉于廊庙之上，以诛其事。敌人开阖，必亟入之，先其所爱，微与之 期，践墨随敌，以决战事。是故始如处女，敌人开户；后如脱兔，敌不及拒。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;用兵之法，有散地，有轻地，有争地，有交地，有衢地，有重地，有泛地，有围地，有死地。诸侯自战其地者，为散地；入人之地不深者，为轻地。我得则利，彼得亦利者，为争地；我可以往，彼可以来者，为交地；诸侯之地三属，先至而得天下众者，为衢地；入人之地深，背城邑多
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：地形第十</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-10.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-10.html</id>
    <published>2019-03-12T10:52:24.000Z</published>
    <updated>2019-03-12T10:52:24.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>地形有通者、有挂者、有支者、有隘者、有险者、有远者。我可以往，彼可以来，曰通。通形者，先居高阳，利粮道，以战则利。可以往，难以返，曰挂。挂形者，敌无备，出而胜之，敌若有备，出而不胜，难以返，不利。我出而不利，彼出而不利，曰支。支形者，敌虽利我，我无出也，引而去之，令敌半出而击之利。隘形者，我先居之，必盈之以待敌。若敌先居之，盈而勿从，不盈而从之。险形者，我先居之，必居高阳以待敌；若敌先居之，引而去之，勿从也。远形者，势均难以挑战，战而不利。凡此六者，地之道也，将之至任，不可不察也。</p><p>凡兵有走者、有驰者、有陷者、有崩者、有乱者、有北者。凡此六者，非天地之灾，将之过也。夫势均，以一击十，曰走；卒强吏弱，曰驰；吏强卒弱，曰陷；大吏怒而不服，遇敌怼而自战，将不知其能，曰崩；将弱不严，教道不明，吏卒无常，陈兵纵横，曰乱；将不能料敌，以少合众，以弱击强，兵无选锋，曰北。凡此六者，败之道也，将之至任，不可不察也。</p><p>夫地形者，兵之助也。料敌制胜，计险隘远近，上将之道也。知此而用战者必胜，不知此而用战者必败。故战道必胜，主曰无战，必战可也；战道不胜，主曰必战，无战可也。故进不求名，退不避罪，唯民是保，而利于主，国之宝也。</p><p>视卒如婴儿，故可以与之赴深溪；视卒如爱子，故可与之俱死。厚而不能使，爱而不能令，乱而不能治，譬若骄子，不可用也。</p><p>知吾卒之可以击，而不知敌之不可击，胜之半也；知敌之可击，而不知吾卒之不可以击，胜之半也；知敌之可击，知吾卒之可以击，而不知地形之不可以战，胜之半也。故知兵者，动而不迷，举而不穷。故曰：知彼知己，胜乃不殆；知天知地，胜乃可全。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;地形有通者、有挂者、有支者、有隘者、有险者、有远者。我可以往，彼可以来，曰通。通形者，先居高阳，利粮道，以战则利。可以往，难以返，曰挂。挂形者，敌无备，出而胜之，敌若有备，出而不胜，难以返，不利。我出而不利，彼出而不利，曰支。支形者，敌虽利我，我无出
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：行军第九</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-9.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-9.html</id>
    <published>2019-03-12T10:52:23.000Z</published>
    <updated>2019-03-12T10:52:23.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>凡处军相敌，绝山依谷，视生处高，战隆无登，此处山之军也。绝水必远水，客绝水而来，勿迎之于水内，令半渡而击之利，欲战者，无附于水而迎客，视生处高，无迎水流，此处水上之军也。绝斥泽，唯亟去无留，若交军于斥泽之中，必依水草而背众树，此处斥泽之军也。平陆处易，右背高，前死后生，此处平陆之军也。凡此四军之利，黄帝之所以胜四帝也。凡军好高而恶下，贵阳而贱阴，养生而处实，军无百疾，是谓必胜。丘陵堤防，必处其阳而右背之，此兵之利，地之助也。上雨水流至，欲涉者，待其定也。凡地有绝涧、天井、天牢、天罗、天陷、天隙，必亟去之，勿近也。吾远之，敌近之；吾迎之，敌背之。军旁有险阻、潢井、蒹葭、小林、□①荟者，必谨覆索之，此伏奸之所处也。</p><p>敌近而静者，恃其险也；远而挑战者，欲人之进也；其所居易者，利也；众树动者，来也；众草多障者，疑也；鸟起者，伏也；兽骇者，覆也；尘高而锐者，车来也；卑而广者，徒来也；散而条达者，樵采也；少而往来者，营军也；辞卑而备者，进也；辞强而进驱者，退也；轻车先出居其侧者，陈也；无约而请和者，谋也；奔走而陈兵者，期也；半进半退者，诱也；杖而立者，饥也；汲而先饮者，渴也；见利而不进者，劳也；鸟集者，虚也；夜呼者，恐也；军扰者，将不重也；旌旗动者，乱也；吏怒者，倦也；杀马肉食者，军无粮也；悬□②不返其舍者，穷寇也；谆谆□③□③，徐与人言者，失众也；数赏者，窘也；数罚者，困也；先暴而后畏其众者，不精之至也；来委谢者，欲休息也。兵怒而相迎，久而不合，又不相去，必谨察之。</p><p>兵非贵益多也，惟无武进，足以并力料敌取人而已。夫惟无虑而易敌者，必擒于人。卒未亲而罚之，则不服，不服则难用。卒已亲附而罚不行，则不可用。故合之以文，齐之以武，是谓必取。令素行以教其民，则民服；令素不行以教其民，则民不服。令素行者，与众相得也。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;凡处军相敌，绝山依谷，视生处高，战隆无登，此处山之军也。绝水必远水，客绝水而来，勿迎之于水内，令半渡而击之利，欲战者，无附于水而迎客，视生处高，无迎水流，此处水上之军也。绝斥泽，唯亟去无留，若交军于斥泽之中，必依水草而背众树，此处斥泽之军也。平陆处易
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：九变第八</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-8.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-8.html</id>
    <published>2019-03-12T10:52:22.000Z</published>
    <updated>2019-03-12T10:52:22.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>凡用兵之法，将受命于君，合军聚合。泛地无舍，衢地合交，绝地无留，围地则谋，死地则战，途有所不由，军有所不击，城有所不攻，地有所不争，君命有所不受。</p><p>故将通于九变之利者，知用兵矣；将不通九变之利，虽知地形，不能得地之利矣；治兵不知九变之术，虽知五利，不能得人之用矣。</p><p>是故智者之虑，必杂于利害，役诸侯者以业，趋诸侯者以利。故用兵之法，无恃其不来，恃吾有以待之；无恃其不攻，恃吾有所不可攻也。</p><p>故将有五危，必死可杀，必生可虏，忿速可侮，廉洁可辱，爱民可烦。凡此五者，将之过也，用兵之灾也。覆军杀将，必以五危，不可不察也。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;凡用兵之法，将受命于君，合军聚合。泛地无舍，衢地合交，绝地无留，围地则谋，死地则战，途有所不由，军有所不击，城有所不攻，地有所不争，君命有所不受。&lt;/p&gt;
&lt;p&gt;故将通于九变之利者，知用兵矣；将不通九变之利，虽知地形，不能得地之利矣；治兵不知九变之术
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：军争第七</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-7.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-7.html</id>
    <published>2019-03-12T10:52:20.000Z</published>
    <updated>2019-03-12T10:52:20.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>凡用兵之法，将受命于君，合军聚众，交和而舍，莫难于军争。军争之难者，以迂为直，以患为利。</p><p>故迂其途，而诱之以利，后人发，先人至，此知迂直之计者也。军争为利，军争为危。举军而争利则不及，委军而争利则辎重捐。是故卷甲而趋，日夜不处，倍道兼行，百里而争利，则擒三将军，劲者先，疲者后，其法十一而至；五十里而争利，则蹶上将军，其法半至；三十里而争利，则三分之二至。是故军无辎重则亡，无粮食则亡，无委积则亡。故不知诸侯之谋者，不能豫交；不知山林、险阻、沮泽之形者，不能行军；不用乡导者，不能得地利。故兵以诈立，以利动，以分和为变者也。故其疾如风，其徐如林，侵掠如火，不动如山，难知如阴，动如雷震。掠乡分众，廓地分利，悬权而动。先知迂直之计者胜，此军争之法也。</p><p>《军政》曰：”言不相闻，故为之金鼓；视不相见，故为之旌旗。”夫金鼓旌旗者，所以一民之耳目也。民既专一，则勇者不得独进，怯者不得独退，此用众之法也。故夜战多金鼓，昼战多旌旗，所以变人之耳目也。</p><p>三军可夺气，将军可夺心。是故朝气锐，昼气惰，暮气归。善用兵者，避其锐气，击其惰归，此治气者也。以治待乱，以静待哗，此治心者也。以近待远，以佚待劳，以饱待饥，此治力者也。无邀正正之旗，无击堂堂之陈，此治变者也。</p><p>故用兵之法，高陵勿向，背丘勿逆，佯北勿从，锐卒勿攻，饵兵勿食 ，归师勿遏，围师遗阙，穷寇勿迫，此用兵之法也。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;凡用兵之法，将受命于君，合军聚众，交和而舍，莫难于军争。军争之难者，以迂为直，以患为利。&lt;/p&gt;
&lt;p&gt;故迂其途，而诱之以利，后人发，先人至，此知迂直之计者也。军争为利，军争为危。举军而争利则不及，委军而争利则辎重捐。是故卷甲而趋，日夜不处，倍道兼行
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：虚实第六</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-6.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-6.html</id>
    <published>2019-03-12T10:51:27.000Z</published>
    <updated>2019-03-12T10:51:27.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>凡先处战地而待敌者佚，后处战地而趋战者劳。故善战者，致人而不致于人。能使敌人自至者，利之也；能使敌人不得至者，害之也。故敌佚能劳之，饱能饥之，安能动之。出其所必趋，趋其所不意。</p><p>行千里而不劳者，行于无人之地也；攻而必取者，攻其所不守也。守而必固者，守其所必攻也。故善攻者，敌不知其所守；善守者，敌不知其所攻。微乎微乎，至于无形；神乎神乎，至于无声，故能为敌之司命。进而不可御者，冲其虚也；退而不可追者，速而不可及也。故我欲战，敌虽高垒深沟，不得不与我战者，攻其所必救也；我不欲战，虽画地而守之，敌不得与我战者，乖其所之也。故形人而我无形，则我专而敌分。我专为一，敌分为十，是以十攻其一也。则我众敌寡，能以众击寡者，则吾之所与战者约矣。吾所与战之地不可知，不可知则敌所备者多，敌所备者多，则吾所与战者寡矣。故备前则后寡，备后则前寡，备左则右寡，备右则左寡，无所不备，则无所不寡。寡者，备人者也；众者，使人备己者也。故知战之地，知战之日，则可千里而会战；不知战之地，不知战日，则左不能救右，右不能救左，前不能救后，后不能救前，而况远者数十里，近者数里乎！</p><p>以吾度之，越人之兵虽多，亦奚益于胜哉！</p><p>故曰：胜可为也。敌虽众，可使无斗。故策之而知得失之计，候之而知动静之理，形之而知死生之地，角之而知有余不足之处。故形兵之极，至于无形。无形则深间不能窥，智者不能谋。因形而措胜于众，众不能知。人皆知我所以胜之形，而莫知吾所以制胜之形。故其战胜不复，而应形于无穷。</p><p>夫兵形象水，水之行避高而趋下，兵之形避实而击虚；水因地而制流，兵因敌而制胜。故兵无常势，水无常形。能因敌变化而取胜者，谓之神。故五行无常胜，四时无常位，日有短长，月有死生。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;凡先处战地而待敌者佚，后处战地而趋战者劳。故善战者，致人而不致于人。能使敌人自至者，利之也；能使敌人不得至者，害之也。故敌佚能劳之，饱能饥之，安能动之。出其所必趋，趋其所不意。&lt;/p&gt;
&lt;p&gt;行千里而不劳者，行于无人之地也；攻而必取者，攻其所不守也。
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>孙子兵法：兵势第五</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-5.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-5.html</id>
    <published>2019-03-12T10:47:12.000Z</published>
    <updated>2019-03-12T10:47:12.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：</p><p>凡治众如治寡，分数是也；斗众如斗寡，形名是也；三军之众，可使必受敌而无败者，奇正是也；兵之所加，如以碬投卵者，虚实是也。</p><p>凡战者，以正合，以奇胜。故善出奇者，无穷如天地，不竭如江海。终而复始，日月是也。死而更生，四时是也。声不过五，五声之变，不可胜听也；色不过五，五色之变，不可胜观也；味不过五，五味之变，不可胜尝也；战势不过奇正，奇正之变，不可胜穷也。奇正相生，如循环之无端，孰能穷之哉！</p><p>激水之疾，至于漂石者，势也；鸷鸟之疾，至于毁折者，节也。故善战者，其势险，其节短。势如扩弩，节如发机。纷纷纭纭，斗乱而不可乱；浑浑沌沌，形圆而不可败。乱生于治，怯生于勇，弱生于强。治乱，数也；勇怯，势也；强弱，形也。</p><p>故善动敌者，形之，敌必从之；予之，敌必取之。以利动之，以卒待之。故善战者，求之于势，不责于人故能择人而任势。任势者，其战人也，如转木石。木石之性，安则静，危则动，方则止，圆则行。</p><p>故善战人之势，如转圆石于千仞之山者，势也。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：&lt;/p&gt;
&lt;p&gt;凡治众如治寡，分数是也；斗众如斗寡，形名是也；三军之众，可使必受敌而无败者，奇正是也；兵之所加，如以碬投卵者，虚实是也。&lt;/p&gt;
&lt;p&gt;凡战者，以正合，以奇胜。故善出奇者，无穷如天地，不竭如江海。终而复始，日月是也。死而更生，四时是也。声不过五，五声
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
  <entry>
    <title>Flutter 开发（09）：Flutter 异步编程</title>
    <link href="https://fiissh.tech/2019/flutter-async-program.html"/>
    <id>https://fiissh.tech/2019/flutter-async-program.html</id>
    <published>2019-03-11T02:16:59.000Z</published>
    <updated>2019-03-11T02:16:59.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍 <code>Flutter</code> 异步编程相关的内容。关键字 <code>async</code>、<code>async</code><sup><em></em></sup>、<code>sync</code>、<code>sync</code><sup></sup>、<code>await</code>、<code>yield</code> 以及 <code>Future</code>、<code>Stream</code> 和 <code>Timer</code> 对象构成了 <code>Flutter</code> 异步编程的全部内容。</p><p><code>Future</code> 是一个泛型对象，表示程序在将来的某个时刻能够获取到一个值，泛型类型表示了该返回值的类型。当返回值是 <code>Future</code> 的方法被调用之后，<code>Flutter</code>（<code>Dart</code>）将会做两件事情：</p><ul><li>将需要执行的的任务添加到异步队列中，并返回一个未完成的 <code>Future</code> 对象</li><li>如果任务的运行结果有效（包括异常信息），<code>Future</code> 以该值完成任务的执行。如果需要使用 <code>Future</code> 中的值，可以使用如下两种方式：<ul><li>使用 <code>async</code> 和 <code>await</code> 关键字</li><li>使用 <code>Future API</code></li></ul></li></ul><p><code>Stream</code> 也是一个泛型对象，表示异步数据的流通管道。它只允许从一端插入数据并通过管道从另外一端流出数据。为了控制 <code>Stream</code>，我们通常使用 <code>StreamController</code> 对 <code>Stream</code> 进行管理：</p><ul><li><code>StreamController</code> 提供了类型为 <code>StreamSink</code> 的 <code>sink</code> 属性作为数据的入口</li><li><code>StreamController</code> 提供了 <code>stream</code> 属性作为数据的出口</li></ul><p>在<code>Dart 1.9</code> 中引入了函数生成器的概念，利用惰性方法计算结果序列，以提升性能。生成器有两种类型：</p><ul><li>同步生成器：在需要的时候才生成值，然后使用者从生成器中拉取。使用 <code>sync</code><sup>*</sup> 表示</li><li>异步生成器：会以它自身的速度生成值，然后推送到使用者可以使用的地方。使用 <code>async</code><sup>*</sup> 表示</li></ul><p><code>Timer</code> 提供了一种使用计时器的异步任务的执行方式，<br><a id="more"></a></p><h2 id="async-和-await"><a href="#async-和-await" class="headerlink" title="async 和 await"></a>async 和 await</h2><p>从 <code>Dart 1.9</code> 开始，<code>Dart</code> 添加了 <code>async</code> 和 <code>await</code> 关键字用来实现异步的功能。它们允许我们编写看起来像是同步代码而且不需要使用 <code>Future</code> 的异步代码。在方法体前面使用 <code>async</code> 关键字修饰即表示这是一个异步方法。对调用者来说，调用 <code>async</code> 方法和其他方法没有什么区别，而且对于 <code>return</code> 返回值来说也没有什么影响。</p><p>非异步调用的方法定义如下：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">String</span> getVersionName() &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'1.0.0'</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用方式：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">String</span> versionName = getVersionName();</span><br></pre></td></tr></table></figure><p><code>async</code> 修饰的异步方法最终将返回值封装为 <code>Future</code> 对象。即：使用 <code>async</code> 修饰的异步方法，其返回值类型需要封装为 <code>Future</code> 对象。<code>getVersionName()</code> 方法修改为异步方法之后定义如下：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;<span class="built_in">String</span>&gt; getVersionName() <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'1.0.0'</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用方式：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">String</span> versionName = <span class="keyword">await</span> getVersionName();</span><br></pre></td></tr></table></figure><p>如果同步方法的返回值类型为 <code>T</code>，那么该方法异步方式的返回值类型应该是 <code>Future&lt;T&gt;</code>。如果同步方法中返回值类型为 <code>Future&lt;T&gt;</code>，那么对应的异步方法的返回值类型同样是 <code>Future&lt;T&gt;</code> 而不是 <code>Future&lt;Future&lt;T&gt;&gt;</code>。</p><p>如果异步方法没有显式的使用 <code>return</code> 返回一个值，那么在 <code>Flutter</code> 中将自动封装一个 <code>Future&lt;Null&gt;</code> 作为返回值。</p><p>需要注意的是，<code>await</code> 只能在 <code>async</code> 修饰的方法中使用，而且可以使用多次。使用 <code>await</code> 修饰的方法会自动进入阻塞状态，一直到任务执行完成并返回对应的值。</p><h2 id="函数生成器"><a href="#函数生成器" class="headerlink" title="函数生成器"></a>函数生成器</h2><p>在 <code>Dart 1.9</code> 中引入了函数生成器的概念，利用惰性函数计算结果序列，以提升性能。</p><p>函数生成器有两种类型：同步生成器和异步生成器。同步生成器在需要的时候才生成值，然后使用者从生成器中拉取。异步生成器会以它自身的速度生成值，然后推送到使用者可以使用的地方。</p><h3 id="同步生成器：sync"><a href="#同步生成器：sync" class="headerlink" title="同步生成器：sync*"></a>同步生成器：sync<sup>*</sup></h3><p>通过在方法主体前添加 <code>sync</code><sup>*</sup> 可以标记该方法为同步生成器。同步生成器返回一个 <code>Iterable&lt;T&gt;</code> 泛型对象，并使用 <code>yield</code> 语句来传递值。以获取第一个自然数 <code>n</code> 为例：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Iterable</span>&lt;<span class="built_in">int</span>&gt; naturalsTo(<span class="built_in">int</span> n) <span class="keyword">sync</span>* &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">"Begin"</span>);</span><br><span class="line">  <span class="built_in">int</span> k = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (k &lt; n) <span class="keyword">yield</span> k++;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">print</span>(<span class="string">"End"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当调用方法 <code>naturalsTo</code> 的时候，会立刻返回 <code>Iterable&lt;int&gt;</code> 对象。在我们调用 <code>iterator.moveNext()</code> 之前，函数的主体并不会执行。当运行该方法时，代码会执行到 <code>yield</code> 关键字修饰的位置，并暂停运行，此时 <code>moveNext()</code> 方法会返回 <code>true</code>。方法会在下一次调用 <code>moveNext()</code> 的时候恢复执行。当循环结束的时候，上述方法会隐式的执行 <code>return</code> 并终止迭代，此时 <code>moveNext()</code> 方法会返回 <code>false</code>。</p><p>我们通过如下代码晚上上述方法的调用：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> iterator = naturalsTo(<span class="number">3</span>).iterator;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (iterator.moveNext()) &#123;</span><br><span class="line">  <span class="built_in">print</span>(iterator.current);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出结果：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Begin</span><br><span class="line">0</span><br><span class="line">1</span><br><span class="line">2</span><br><span class="line">End</span><br></pre></td></tr></table></figure><h3 id="异步生成器：async"><a href="#异步生成器：async" class="headerlink" title="异步生成器：async*"></a>异步生成器：async<sup>*</sup></h3><p>通过在方法主体前添加 <code>async</code><sup>*</sup> 可以标记该方法为异步生成器。异步生成器返回一个 <code>Stream</code> 对象，并使用 <code>yield</code> 语句来传递值。以自然数生成器为例：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Stream&lt;<span class="built_in">int</span>&gt; asynchronousNaturalsTo(<span class="built_in">int</span> n) <span class="keyword">async</span>* &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">"Begin"</span>);</span><br><span class="line">  <span class="built_in">int</span> k = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (k &lt; n) <span class="keyword">yield</span> k++;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">"End"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过如下代码完成调用：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">asynchronousNaturalsTo(<span class="number">3</span>).listen((v) &#123;</span><br><span class="line">  <span class="built_in">print</span>(v);</span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">/// outputs</span></span><br><span class="line"><span class="comment">/// I/flutter (19802): Begin</span></span><br><span class="line"><span class="comment">/// I/flutter (19802): 0</span></span><br><span class="line"><span class="comment">/// I/flutter (19802): 1</span></span><br><span class="line"><span class="comment">/// I/flutter (19802): 2</span></span><br><span class="line"><span class="comment">/// I/flutter (19802): End</span></span><br></pre></td></tr></table></figure><p>当调用 <code>asynchronousNaturalsTo</code> 方法的时候，程序会立刻返回 <code>Stream&lt;int&gt;</code> 对象，但是函数体并没有执行。一旦开始 <code>listen</code> 监听数据流，方法体就开始执行。当执行到 <code>yield</code> 修饰的位置的时候，会将 <code>yield</code> 修饰的表达式的运算结果添加到 <code>Stream</code> 数据流中。异步生成器没有必要暂停，因为数据流可以通过 <code>StreamSubscription</code> 进行控制：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">StreamSubscription&lt;int&gt; subscription = asynchronousNaturalsTo(3).listen(null);</span><br><span class="line">subscription.onData((value) &#123;</span><br><span class="line">  print(value);</span><br><span class="line">  if (value == 1) &#123;</span><br><span class="line">    subscription.pause();</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><p>我们可以通过 <code>StreamSubscription</code> 来控制 <code>Stream</code> 的暂停(<code>subscription.pause()</code>)和取消(<code>subscription.cancel()</code>)。如果被暂停，方法会执行到 <code>yield</code> 修饰的位置，然后暂停，直到我们通过 <code>subscription.resume()</code> 恢复 <code>Stream</code>。</p><h3 id="yield-和-yield"><a href="#yield-和-yield" class="headerlink" title="yield 和 yield*"></a>yield 和 yield<sup>*</sup></h3><p>在上述的示例中，可以使用 <code>yield</code> 方便的传递值。如果我们的生成器函数中使用到了递归调用，那么使用 <code>yield</code> 将会带来一些性能问题。以从大到小获取自然数的的代码为例：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Iterable</span>&lt;<span class="built_in">int</span>&gt; naturalsDownFrom(<span class="built_in">int</span> n) <span class="keyword">sync</span>* &#123;</span><br><span class="line">  <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">yield</span> n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">int</span> i <span class="keyword">in</span> naturalsDownFrom(n - <span class="number">1</span>)) &#123;</span><br><span class="line">      <span class="keyword">yield</span> i;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述代码通过递归调用的方式获取自然数。需要注意的是，我们每次调用 <code>naturalsDownFrom</code> 方法都会构建一个新的序列，并通过遍历新的序列然后使用 <code>yield</code> 将元素插入到当前序列中。我们在代码的一些位置插入输出来进行分析：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">int</span> level = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">Iterable</span>&lt;<span class="built_in">int</span>&gt; naturalsDownFrom(<span class="built_in">int</span> n) <span class="keyword">sync</span>* &#123;</span><br><span class="line">  <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"Begin"</span>);</span><br><span class="line">    level = level + <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">'当前 n 为：<span class="subst">$n</span>  level 执行次数：<span class="subst">$level</span>'</span>);</span><br><span class="line">    <span class="keyword">yield</span> n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">int</span> i <span class="keyword">in</span> naturalsDownFrom(n - <span class="number">1</span>)) &#123;</span><br><span class="line">      <span class="built_in">print</span>(<span class="string">'当前 i 为：<span class="subst">$i</span>  level 执行次数：<span class="subst">$level</span>'</span>);</span><br><span class="line">      <span class="keyword">yield</span> i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"End"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出如下：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">I/flutter (31880): Begin</span><br><span class="line">I/flutter (31880): 当前 n 为：3  level 执行次数：1</span><br><span class="line">I/flutter (31880): Begin</span><br><span class="line">I/flutter (31880): 当前 n 为：2  level 执行次数：2</span><br><span class="line">I/flutter (31880): 当前 i 为：2  level 执行次数：2</span><br><span class="line">I/flutter (31880): Begin</span><br><span class="line">I/flutter (31880): 当前 n 为：1  level 执行次数：3</span><br><span class="line">I/flutter (31880): 当前 i 为：1  level 执行次数：3</span><br><span class="line">I/flutter (31880): 当前 i 为：1  level 执行次数：3</span><br><span class="line">I/flutter (31880): End</span><br><span class="line">I/flutter (31880): End</span><br></pre></td></tr></table></figure><p>在上述代码中：</p><ul><li>当 <code>n</code> 为3时，只执行了一次 <code>yield n</code></li><li>当 <code>n</code> 为2时，先执行了一次 <code>yield n</code>，然后执行了一次 <code>yield i</code>(实际上是执行了一次 <code>n = 3</code> 时的逻辑)</li><li>当 <code>n</code> 为1时，先执行了一次 <code>yield n</code>，然后执行了两次 <code>yield i</code>（实际上是分别执行了 <code>n = 3</code> 和 <code>n = 2</code> 时的逻辑）</li></ul><p>也就是说，这种方式下，使用 <code>yield</code> 传递值的次数总共执行了 $x(x-1)/2$ 次，时间复杂度为 $O(n^2)$，存在明显的性能问题。</p><p>我们使用 <code>yield</code><sup>*</sup> 对原来的递归调用进行修改：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">int</span> level = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Iterable</span>&lt;<span class="built_in">int</span>&gt; naturalsDownFrom(<span class="built_in">int</span> n) <span class="keyword">sync</span>* &#123;</span><br><span class="line">  <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"Begin"</span>);</span><br><span class="line">    level = level + <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">'当前 n 为：<span class="subst">$n</span>  level 执行次数：<span class="subst">$level</span>'</span>);</span><br><span class="line">    <span class="keyword">yield</span> n;</span><br><span class="line">    <span class="keyword">yield</span>* naturalsDownFrom(n - <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"End"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出如下：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">I/flutter (32505): Begin</span><br><span class="line">I/flutter (32505): 当前 n 为：3  level 执行次数：1</span><br><span class="line">I/flutter (32505): Begin</span><br><span class="line">I/flutter (32505): 当前 n 为：2  level 执行次数：2</span><br><span class="line">I/flutter (32505): Begin</span><br><span class="line">I/flutter (32505): 当前 n 为：1  level 执行次数：3</span><br><span class="line">I/flutter (32505): End</span><br><span class="line">I/flutter (32505): End</span><br></pre></td></tr></table></figure><p>使用 <code>yield</code><sup><em></em></sup> 调整之后，通过 <code>yield</code> 传递值的过程只有三次。<code>yield</code><sup></sup> 的作用是：将 <code>yield</code><sup><em></em></sup> 后面的子序列（在 <code>sync</code><sup></sup> 方法中，子序列必须是一个 <code>Iterable</code> 可迭代对象；在 <code>async</code><sup>*</sup> 方法中，子序列必须是一个 <code>Stream</code> 数据流）的所有元素插入到当前创建的序列中，而不是创建新的序列。</p><p>综上，<code>yield</code> 在递归调用中造成性能开销过大的本质原因是，生成器函数在每次调用的时候都会生成一个新的序列。在递归调用中，需要额外的开销将子序列的元素插入到当前序列中。</p><h2 id="Future"><a href="#Future" class="headerlink" title="Future"></a>Future</h2><p>如果要使用 <code>Future</code>，需要在代码中导入相应的库：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'dart:async'</span>;</span><br></pre></td></tr></table></figure><p><code>Future</code> 表示一个异步任务在将来执行时产生的结果。<code>Flutter</code> 使用的 <code>Dart</code> 语言是单线程模型的语言，在 <code>Flutter</code> 中使用 <code>isolate</code>（协程）来表示一个异步任务。在 <code>Flutter</code> 线程的消息机制中，有一个消息循环（<code>event loop</code>）和两个队列（<code>event queue</code> 和 <code>microtask queue</code>）：</p><ul><li><code>event queue</code> 中包含所有的的外来事件，包括 <code>I/O</code>、<code>mouse events</code>、<code>drawing events</code>、<code>timers</code> 以及 <code>isolate</code> 之间的消息等。在任意的 <code>isolate</code> 中新增的事件都会放入到 <code>even loop</code> 中等待执行</li><li><code>microtask queue</code> 只在当前 <code>isolate</code> 的任务队列中排队，优先级高于 <code>event queue</code>，主要是通过 <code>scheduleMicrotask</code> 来调度</li></ul><p><img src="https://upload-images.jianshu.io/upload_images/1975877-7f5e775270e16dad?imageMogr2/auto-orient/strip%7CimageView2/2/w/471" alt></p><p><code>Flutter</code> 中的事件处理遵循如下规则：</p><ul><li>首先处理 <code>microtask queue</code> 中的任务（事件）</li><li>处理完 <code>microtask queue</code> 中的所有任务之后，从 <code>event queue</code> 中取出一个任务进行处理</li><li>回到 <code>microtask queue</code>，处理其中的任务，然后重复上述过程</li></ul><p>那么 <code>Flutter</code> 中是如何实现异步任务的呢？我们只需要将任务放入到 <code>microtask queue</code> 或者 <code>event queue</code> 中即可。通过 <code>Future.microtask</code> 接口（实际上使用了 <code>scheduleMicrotask</code>）可以将任务放置在 <code>microtask queue</code>。而通过 <code>Future</code> 的构造方法 <code>Future(FutureOr&lt;T&gt; computation())</code> 创建的任务则放置到了 <code>event queue</code> 中（实际上是通过 <code>Timer.run</code> 实现的）。</p><h3 id="创建异步（同步）任务"><a href="#创建异步（同步）任务" class="headerlink" title="创建异步（同步）任务"></a>创建异步（同步）任务</h3><p><code>Future</code> 中提供了多个接口用于创建异步或同步任务：</p><ul><li>使用 <code>factory Future(FutureOr&lt;T&gt; computation())</code>：该方法可以在 <code>event loop</code> 中创建一个异步任务</li></ul><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future(() =&gt; <span class="built_in">print</span>(<span class="string">'在 event queue 中运行的 Future'</span>));</span><br></pre></td></tr></table></figure><ul><li>使用 <code>factory Future.delayed(Duration duration, [FutureOr&lt;T&gt; computation()])</code>：该方法可以在 <code>event loop</code> 中创建一个延时执行的异步任务</li></ul><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future.delayed(<span class="keyword">const</span> <span class="built_in">Duration</span>(seconds:<span class="number">1</span>), () =&gt; <span class="built_in">print</span>(<span class="string">'1秒后在 event queue 中运行的 Future'</span>));</span><br></pre></td></tr></table></figure><ul><li>使用 <code>factory Future.microtask(FutureOr&lt;T&gt; computation())</code>：该方法可以在 <code>microtask queue</code> 中创建一个异步任务</li></ul><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future.microtask(() =&gt; <span class="built_in">print</span>(<span class="string">'在 microtask queue 里运行的 Future'</span>));</span><br></pre></td></tr></table></figure><ul><li>使用 <code>factory Future.sync(FutureOr&lt;T&gt; computation())</code>：该方法可以创建一个同步任务</li></ul><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future.<span class="keyword">sync</span>(() =&gt; <span class="built_in">print</span>(<span class="string">'同步运行的Future'</span>));</span><br></pre></td></tr></table></figure><p>需要注意的是，<code>Future.sync</code> 创建的同步任务指的是构造 <code>Future</code> 是传入的 <code>computation()</code> 对象是同步执行的。但是通过 <code>then</code> 注册的回调方法是在 <code>microtask queue</code> 中执行的。</p><h4 id="Completer"><a href="#Completer" class="headerlink" title="Completer"></a>Completer</h4><p><code>future.dart</code> 中有一个很特殊的类：<code>Completer</code>。我们通过 <code>Future</code> 的接口向队列添加任务之后，我们只能被动的通过注册的回调方法接收结果或者处理错误。而 <code>Completer</code> 提供了一系列接口允许我们对 <code>Future</code> 的执行过程进行控制。一个比较好的示例是 <a href="https://github.com/dart-lang/http/blob/21e02c10c042cbd396d39b825dc7538ea6cccab8/lib/src/utils.dart" target="_blank" rel="noopener">http/http.dart 库</a> 中的 <code>store</code> 方法：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Future store(Stream stream, EventSink sink) &#123;</span><br><span class="line">  <span class="keyword">var</span> completer = <span class="keyword">new</span> Completer();</span><br><span class="line">  stream.listen(sink.add, onError: sink.addError, onDone: () &#123;</span><br><span class="line">    sink.close();</span><br><span class="line">    completer.complete();</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> completer.future;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="then"><a href="#then" class="headerlink" title="then"></a>then</h3><p><code>Future</code>通过向 <code>then()</code> 方法注册回调方法来接收返回的结果：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;R&gt; then&lt;R&gt;(FutureOr&lt;R&gt; onValue(T value), &#123;<span class="built_in">Function</span> onError&#125;);</span><br></pre></td></tr></table></figure><p>如果任务正确执行并返回了正确的值（非异常值），<code>onValue</code> 回调方法会被调用。此时通过 <code>value</code> 获取对应的返回值。另外，<code>then</code> 方法中还有一个可选的 <code>onError</code> 回调方法用于处理 <code>onValue</code> 回调方法执行过程中出现的异常信息。如果不注册该回调方法，那么将默认通过 <code>catchError</code> 返回异常信息。</p><p>需要注意的是，<code>onError</code> 的回调方法需要提供一个或两个参数，其中第二个参数为 可选的 <code>StackTrace</code> 对象。</p><p>需要注意的是，在肥肥测试的过程中 <code>onError</code> 并没有被执行，而是将异常信息交给了 <code>catchError</code> 进行处理。目前不确定是何种原因造成的问题。测试使用的代码如下：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">getVersionName().then((value) &#123;</span><br><span class="line">  <span class="built_in">print</span>(value);</span><br><span class="line">  <span class="built_in">String</span> result = <span class="keyword">null</span>;</span><br><span class="line">  result.substring(<span class="number">0</span>, <span class="number">4</span>);</span><br><span class="line">&#125;, onError: (<span class="built_in">Object</span> e, StackTrace st) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'出现异常 onError： &#123;e.toString()&#125;'</span>);</span><br><span class="line">&#125;).catchError((<span class="built_in">Object</span> e) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'出现异常 catchError：&#123;e.toString()&#125;'</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h3 id="catchError"><a href="#catchError" class="headerlink" title="catchError"></a>catchError</h3><p><code>Future</code> 通过向 <code>catchError</code> 接口注册回调方法的方式来处理异常：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;T&gt; catchError(<span class="built_in">Function</span> onError, &#123;<span class="built_in">bool</span> test(<span class="built_in">Object</span> error)&#125;);</span><br></pre></td></tr></table></figure><p>异步任务执行的过程中出现的异常信息将会由 <code>catchError</code> 进行处理。</p><p>我们也可以通过 <code>Future.error</code> 返回错误信息，该错误信息也会由 <code>catchError</code> 进行处理。</p><p>另外，<code>catchError</code> 方法还有一个可选的 <code>test</code> 参数用于接收一个回调。如果该方法返回 <code>true</code>，那么 <code>catchError</code> 捕获的异常信息将会先由 <code>test</code> 参数对应的回调方法进行响应，然后再由 <code>onError</code> 参数对应的回调方法进行响应。如果返回 <code>false</code>，那么 <code>test</code> 参数对应的回调执行之后，<code>onError</code> 参数对应的回调则不再执行，应用程序将会抛出 <code>Unhandled Exception</code>。</p><h3 id="whenComplete"><a href="#whenComplete" class="headerlink" title="whenComplete"></a>whenComplete</h3><p><code>Future</code> 通过向 <code>whenComplete</code> 接口注册回调方法的方式来保证特定的代码逻辑一定能够得到执行。</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;T&gt; whenComplete(FutureOr action());</span><br></pre></td></tr></table></figure><p>与使用 <code>try-catch-finally</code> 捕获异常信息类似的，<code>Future</code> 可以通过 <code>whenComplete</code> 的回调方法保证无论何种情况都会保证代码的执行。</p><h3 id="Future-wait"><a href="#Future-wait" class="headerlink" title="Future.wait"></a>Future.wait</h3><p>如果需要调用多个异步方法并且一并将返回值返回，可以使用 <code>Future.wait</code> 接口。当所有 <code>Future</code> 完成后，返回 <code>List</code> 类型的值列表：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> Future&lt;<span class="built_in">List</span>&lt;T&gt;&gt; wait&lt;T&gt;(<span class="built_in">Iterable</span>&lt;Future&lt;T&gt;&gt; futures, &#123;<span class="built_in">bool</span> eagerError: <span class="keyword">false</span>, <span class="keyword">void</span> cleanUp(T successValue)&#125;)</span><br></pre></td></tr></table></figure><h3 id="Future-doWhile"><a href="#Future-doWhile" class="headerlink" title="Future.doWhile"></a>Future.doWhile</h3><p>如果需要执行循环的任务，可以使用 <code>Future.doWhile</code>。只有当执行结果返回 <code>false</code> 的时候才会停止：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> Future doWhile(FutureOr&lt;<span class="built_in">bool</span>&gt; action())</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文主要介绍 &lt;code&gt;Flutter&lt;/code&gt; 异步编程相关的内容。关键字 &lt;code&gt;async&lt;/code&gt;、&lt;code&gt;async&lt;/code&gt;&lt;sup&gt;&lt;em&gt;&lt;/em&gt;&lt;/sup&gt;、&lt;code&gt;sync&lt;/code&gt;、&lt;code&gt;sync&lt;/code&gt;&lt;sup&gt;&lt;/sup&gt;、&lt;code&gt;await&lt;/code&gt;、&lt;code&gt;yield&lt;/code&gt; 以及 &lt;code&gt;Future&lt;/code&gt;、&lt;code&gt;Stream&lt;/code&gt; 和 &lt;code&gt;Timer&lt;/code&gt; 对象构成了 &lt;code&gt;Flutter&lt;/code&gt; 异步编程的全部内容。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;Future&lt;/code&gt; 是一个泛型对象，表示程序在将来的某个时刻能够获取到一个值，泛型类型表示了该返回值的类型。当返回值是 &lt;code&gt;Future&lt;/code&gt; 的方法被调用之后，&lt;code&gt;Flutter&lt;/code&gt;（&lt;code&gt;Dart&lt;/code&gt;）将会做两件事情：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;将需要执行的的任务添加到异步队列中，并返回一个未完成的 &lt;code&gt;Future&lt;/code&gt; 对象&lt;/li&gt;
&lt;li&gt;如果任务的运行结果有效（包括异常信息），&lt;code&gt;Future&lt;/code&gt; 以该值完成任务的执行。如果需要使用 &lt;code&gt;Future&lt;/code&gt; 中的值，可以使用如下两种方式：&lt;ul&gt;
&lt;li&gt;使用 &lt;code&gt;async&lt;/code&gt; 和 &lt;code&gt;await&lt;/code&gt; 关键字&lt;/li&gt;
&lt;li&gt;使用 &lt;code&gt;Future API&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;Stream&lt;/code&gt; 也是一个泛型对象，表示异步数据的流通管道。它只允许从一端插入数据并通过管道从另外一端流出数据。为了控制 &lt;code&gt;Stream&lt;/code&gt;，我们通常使用 &lt;code&gt;StreamController&lt;/code&gt; 对 &lt;code&gt;Stream&lt;/code&gt; 进行管理：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;StreamController&lt;/code&gt; 提供了类型为 &lt;code&gt;StreamSink&lt;/code&gt; 的 &lt;code&gt;sink&lt;/code&gt; 属性作为数据的入口&lt;/li&gt;
&lt;li&gt;&lt;code&gt;StreamController&lt;/code&gt; 提供了 &lt;code&gt;stream&lt;/code&gt; 属性作为数据的出口&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在&lt;code&gt;Dart 1.9&lt;/code&gt; 中引入了函数生成器的概念，利用惰性方法计算结果序列，以提升性能。生成器有两种类型：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;同步生成器：在需要的时候才生成值，然后使用者从生成器中拉取。使用 &lt;code&gt;sync&lt;/code&gt;&lt;sup&gt;*&lt;/sup&gt; 表示&lt;/li&gt;
&lt;li&gt;异步生成器：会以它自身的速度生成值，然后推送到使用者可以使用的地方。使用 &lt;code&gt;async&lt;/code&gt;&lt;sup&gt;*&lt;/sup&gt; 表示&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;Timer&lt;/code&gt; 提供了一种使用计时器的异步任务的执行方式，&lt;br&gt;
    
    </summary>
    
      <category term="Flutter" scheme="https://fiissh.tech/categories/flutter/"/>
    
    
      <category term="Flutter 异步编程" scheme="https://fiissh.tech/tags/flutter-%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/"/>
    
      <category term="await" scheme="https://fiissh.tech/tags/await/"/>
    
      <category term="sync" scheme="https://fiissh.tech/tags/sync/"/>
    
      <category term="Future" scheme="https://fiissh.tech/tags/future/"/>
    
  </entry>
  
  <entry>
    <title>Android Studio 编译 Resolved Versions for App (26.1.0) and Test App (27.1.1) Differ 错误</title>
    <link href="https://fiissh.tech/2019/resolved-version-for-app-and-test-app-differ-error.html"/>
    <id>https://fiissh.tech/2019/resolved-version-for-app-and-test-app-differ-error.html</id>
    <published>2019-03-08T04:04:59.000Z</published>
    <updated>2019-03-08T04:04:59.000Z</updated>
    
    <content type="html"><![CDATA[<p><code>AndroidStudio</code> 新建项目，编译时报错：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Conflict with dependency 'com.android.support:support-annotations' in project ':app'. Resolved versions for app (26.1.0) and test app (27.1.1) differ. See https://d.android.com/r/tools/test-apk-dependency-conflicts.html for details.</span><br></pre></td></tr></table></figure><p>从报错信息来看，是 <code>app</code> 和 <code>test</code> 的依赖版本不一致造成的。项目 <code>build.gradle</code> 中的 <code>dependencies</code> 如下：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    implementation fileTree(include: ['*.jar'], dir: 'libs')</span><br><span class="line">    implementation 'com.android.support:appcompat-v7:26.1.0'</span><br><span class="line">    implementation 'com.android.support:support-v4:26.1.0'</span><br><span class="line">    implementation 'com.android.support:design:26.1.0'</span><br><span class="line">    implementation 'com.android.support.constraint:constraint-layout:1.1.3'</span><br><span class="line">    testImplementation 'junit:junit:4.12'</span><br><span class="line">    androidTestImplementation 'com.android.support.test:runner:1.0.2'</span><br><span class="line">    androidTestImplementation 'com.android.support.test.espresso:espresso-core:3.0.2'</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>目前来看，应该是 <code>support</code> 包与 <code>test</code> 包的版本不一致造成的。解决方案如下：</p><p>方案一：升级 <code>compileSdkVersion</code> 到 27（目前为 26），并将 <code>support</code> 包版本升级到 27</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">implementation 'com.android.support:appcompat-v7:27.1.1'</span><br><span class="line">implementation 'com.android.support:support-v4:27.1.1'</span><br><span class="line">implementation 'com.android.support:design:27.1.1'</span><br></pre></td></tr></table></figure><p>方案二：将 <code>test</code> 包版本降低为 26 对应的版本：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">androidTestImplementation 'com.android.support.test:runner:1.0.1'</span><br><span class="line">androidTestImplementation 'com.android.support.test.espresso:espresso-core:3.0.1'</span><br></pre></td></tr></table></figure><p>方案三：在 <code>build.gradle</code> 文件的 <code>android{ }</code> 属性前增加如下代码：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">configurations.all &#123; resolutionStrategy.force 'com.android.support:support-annotations:26.1.0' &#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;code&gt;AndroidStudio&lt;/code&gt; 新建项目，编译时报错：&lt;/p&gt;
&lt;figure class=&quot;highlight shell&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;
      
    
    </summary>
    
      <category term="Android Studio Exception" scheme="https://fiissh.tech/categories/android-studio-exception/"/>
    
    
      <category term="Resolved versions differ" scheme="https://fiissh.tech/tags/resolved-versions-differ/"/>
    
  </entry>
  
  <entry>
    <title>孙子兵法：军形第四</title>
    <link href="https://fiissh.tech/2019/the-art-of-war-4.html"/>
    <id>https://fiissh.tech/2019/the-art-of-war-4.html</id>
    <published>2019-03-07T10:58:28.000Z</published>
    <updated>2019-03-07T10:58:28.000Z</updated>
    
    <content type="html"><![CDATA[<p>孙子曰：　</p><p>昔之善战者，先为不可胜，以待敌之可胜。不可胜在己，可胜在敌。故善战者，能为不可胜，不能使敌之必可胜。故曰：胜可知，而不可为。不可胜者，守也；可胜者，攻也。守则不足，攻则有余。善守者藏于九地之下，善攻者动于九天之上，故能自保而全胜也。见胜不过众人之所知，非善之善者也；战胜而天下曰善，非善之善者也。故举秋毫不为多力，见日月不为明目，闻雷霆不为聪耳。古之所谓善战者，胜于易胜者也。故善战者之胜也，无智名，无勇功，故其战胜不忒。不忒者，其所措胜，胜已败者也。故善战者，立于不败之地，而不失敌之败也。是故胜兵先胜而后求战，败兵先战而后求胜。善用兵者，修道而保法，故能为胜败之政。　</p><p>兵法：一曰度，二曰量，三曰数，四曰称，五曰胜。地生度，度生量，量生数，数生称，称生胜。故胜兵若以镒称铢，败兵若以铢称镒。　</p><p>称胜者之战民也，若决积水于千仞之溪者，形也。　</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;孙子曰：　&lt;/p&gt;
&lt;p&gt;昔之善战者，先为不可胜，以待敌之可胜。不可胜在己，可胜在敌。故善战者，能为不可胜，不能使敌之必可胜。故曰：胜可知，而不可为。不可胜者，守也；可胜者，攻也。守则不足，攻则有余。善守者藏于九地之下，善攻者动于九天之上，故能自保而全胜也。见胜不过众人之所
      
    
    </summary>
    
      <category term="孙子兵法" scheme="https://fiissh.tech/categories/%E5%AD%99%E5%AD%90%E5%85%B5%E6%B3%95/"/>
    
    
  </entry>
  
</feed>
